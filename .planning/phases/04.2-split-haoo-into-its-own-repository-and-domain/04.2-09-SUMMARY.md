---
phase: 04.2-split-haoo-into-its-own-repository-and-domain
plan: 09
subsystem: infra
tags: [github-actions, github-pages, posthog, tls, planning-record, requirements]

requires:
  - phase: 04.2-08
    provides: "The 26-entry scaffold allowlist and the disjointness auditor, byte-identical in both repositories, with SPLT-01 closed on a green run"
  - phase: 04.2-07
    provides: "The scriptless recovery document at the retired path and the artifact-level no-measurement scan"
  - phase: 04.2-06
    provides: "The reduced ZERO-PAPER HUB tree, with the four VITE_HAOO_* declarations and their readers removed in one commit"
provides:
  - "The three PostHog build variables on KaruguDev/HAOO and on no other repository — the configuration-level half of the separation claim"
  - "HTTPS enforced on www.haoo.online with a certificate naming both legs, read and recorded with timestamps rather than asserted"
  - "The phase 04.2 planning record present in KaruguDev/HAOO, synced one-directionally and proven a byte-for-byte superset over 239 files"
  - "G-04.1-3's capturing evidence re-taken against the hostname that ships, appended without editing the original"
  - "All eight Phase 04.2 requirement statuses set from measured evidence, with five items carried forward with an owner and a phase"
  - "A scheduled, executable follow-up for the ZERO-PAPER HUB planning-directory removal with owner, trigger and exact command"
affects: [05-prove-the-deployed-journey, 04.1-migrate-measurement-from-plausible-to-posthog]

actuals:
  tokens: 30000
  tasks: 3
  commits: 5

tech-stack:
  added: []
  patterns:
    - "Measure the discriminator, not the concatenation: a bundle check that greps every asset at once passes on the vendor chunk's own default and cannot fail"
    - "Copy a configuration value through stdin from captured JSON and compare with cmp BEFORE deleting the source, so a wrong key is impossible rather than unlikely"
    - "Read a wall-clock-dependent platform state before attempting to change it, and record the reading with a timestamp whatever it says"
    - "A one-directional sync asserts the upstream restatement by CONTENT after the copy, because file identity alone passes on a sync that copied a reverted file"

key-files:
  created:
    - "ZPH: .planning/phases/04.2-.../04.2-09-SUMMARY.md"
  modified:
    - "ZPH: .planning/REQUIREMENTS.md (eight statuses, the evidence table, the carry-forward table, the recounted tally)"
    - "ZPH: .planning/ROADMAP.md (phase complete 9/9, the post-verification item, two stale host literals)"
    - "ZPH: .planning/phases/04.1-.../04.1-VERIFICATION.md (appended: re-taken G-04.1-3 evidence, 75 insertions / 0 deletions)"
    - "ZPH: .planning/phases/04.1-.../04.1-USER-SETUP.md (inserted: a superseding pointer beside the 2026-09-05 closure, 12 insertions / 0 deletions)"
    - "ZPH: .planning/phases/04.2-.../04.2-DEFERRED-ITEMS.md (D34-D40 plus two measured records, 424 insertions / 0 deletions)"
    - "ZPH: .planning/phases/04.2-.../04.2-09-PLAN.md (the double-prefixed repository slug, four occurrences)"
    - "HAOO: .planning/ (24 paths — 15 new, 9 updated, 0 deleted)"
    - "GitHub configuration: three repository variables created on KaruguDev/HAOO, three deleted from KaruguDev/ZERO-PAPERHUB"

key-decisions:
  - "The plan's own bundle verification was replaced with the project-chunk measurement its acceptance criterion actually asks for, because the command as written greps a concatenation that includes the vendor SDK chunk and therefore cannot fail"
  - "SPLT-04 is Complete on the truth of the visitor-facing statements; the Kenya DPA 2019 sign-off is a different claim and is carried forward OPEN, exactly as the owner stated at 04.2-04's gate"
  - "The task-3 status-ceiling check was recorded as unsatisfiable rather than satisfied, because the only two ways to make it pass are downgrading a true Phase 1 status or deleting the sentence that records why four IDs moved"
  - "04.2-02-PLAN.md's four occurrences of the same double-prefix defect were left unedited: it is a completed plan's historical record, and rewriting it would make its summary describe a document that no longer exists"

patterns-established:
  - "Absent-then-present on the same artifact is the evidence; either half alone is an assertion that was always going to pass"
  - "Record a counting-method difference (grep -c counts lines; minified JS is one line) whenever two measurements of the same byte-identical file disagree"

requirements-completed: [SPLT-01, SPLT-02, SPLT-03, SPLT-04, PROD-02, PROD-05, PROD-06, QUAL-04]

coverage:
  - id: D1
    description: "The three PostHog build values are repository variables on KaruguDev/HAOO and on no other repository, values byte-identical to the originals"
    requirement: "SPLT-03"
    verification:
      - kind: integration
        ref: "gh variable list --repo KaruguDev/HAOO --json name --jq '.[].name' | sort | tr '\\n' ' ' | grep -q 'VITE_HAOO_MEASUREMENT_PROVIDER VITE_HAOO_POSTHOG_API_HOST VITE_HAOO_POSTHOG_TOKEN'"
        status: pass
      - kind: integration
        ref: "gh variable list --repo KaruguDev/ZERO-PAPERHUB --json name --jq '.[].name' | grep -c VITE_HAOO_ | grep -qx 0"
        status: pass
      - kind: integration
        ref: "cmp <sorted name/value JSON from source> <same from destination> -> exit 0, run BEFORE the originals were deleted"
        status: pass
      - kind: integration
        ref: "gh secret list on both repositories -> empty, so no report-credential name exists as a secret anywhere"
        status: pass
    human_judgment: false
  - id: D2
    description: "A capturing HAOO deploy is confirmed against the final hostname by the inlined approved ingestion origin — absent before the variables existed, present after"
    requirement: "SPLT-03"
    verification:
      - kind: integration
        ref: "fetch https://www.haoo.online/ -> resolve project chunk /assets/haoo-DGFKBCjE.js -> 207,627 bytes non-empty -> contains https://us.i.posthog.com (derived from config/approved-analytics-hosts.ts)"
        status: pass
      - kind: integration
        ref: "04.2-02 measured the same chunk position at 0 hits on 2026-09-05 (/assets/haoo-Di1q_TNO.js, recorded in 04.2-DEFERRED-ITEMS.md)"
        status: pass
      - kind: unit
        ref: "src/test/build-output.test.ts#report credential out of the deploy workflow"
        status: pass
    human_judgment: false
  - id: D3
    description: "The reduced ZERO-PAPER HUB artifact carries no measurement code, no ingestion origin and no credential shape"
    requirement: "SPLT-03"
    verification:
      - kind: integration
        ref: "fetch https://www.zero-paperhub.com/ -> 1,676 bytes (non-empty gate first) -> /assets/main-ClJKpN3o.js 179,611 bytes -> 0 ingestion origins, 0 phc_ keys, 0 case-insensitive posthog, 0 posthog-sdk references"
        status: pass
      - kind: unit
        ref: "src/test/build-output.test.ts#carries no measurement (ZPH)"
        status: pass
    human_judgment: false
  - id: D4
    description: "HTTPS is enforced on the HAOO Pages site and the certificate names its host, read and recorded with a timestamp rather than assumed"
    requirement: "QUAL-04"
    verification:
      - kind: integration
        ref: "gh api repos/KaruguDev/HAOO/pages @ 2026-09-06T14:12:41Z -> cert_state=approved, https_enforced=true, protected_domain_state=verified, cert_domains=[www.haoo.online, haoo.online]"
        status: pass
      - kind: integration
        ref: "openssl s_client @ 2026-09-06T14:13:03Z -> subject CN=www.haoo.online, SAN covers both legs; http->https 301 on both legs, apex->www 301, www 200"
        status: pass
    human_judgment: false
  - id: D5
    description: "G-04.1-3's capturing evidence re-taken against www.haoo.online on all four counts, appended without editing the original finding"
    verification:
      - kind: integration
        ref: "git rev-list --count @{u}..HEAD -> 0; three variables on the building repository; deploy run 34038506835 -> success; approved origin PRESENT in the published project chunk"
        status: pass
      - kind: other
        ref: "git diff --numstat .planning/phases/04.1-.../04.1-VERIFICATION.md -> 75 insertions, 0 deletions"
        status: pass
    human_judgment: false
  - id: D6
    description: "The phase 04.2 planning record exists in KaruguDev/HAOO, synced one-directionally, with the upstream restatements surviving and the destination proven a superset"
    verification:
      - kind: integration
        ref: "cmp over all 239 files under ZPH .planning/ against the HAOO copy -> 0 mismatches, compared count printed and > 0"
        status: pass
      - kind: integration
        ref: "diff .planning/REQUIREMENTS.md HAOO/.planning/REQUIREMENTS.md -> no difference; the MEAS-08 cutover clause asserted present by CONTENT in the destination, and present in the source BEFORE the copy"
        status: pass
      - kind: integration
        ref: "cd HAOO && npm run verify:coverage -> exit 0 (69 capabilities, 3 tables)"
        status: pass
    human_judgment: false
  - id: D7
    description: "shared-scaffold.txt and scripts/verify-tree-disjointness.mjs remain byte-identical across both checkouts and the disjointness auditor stays green in both after the sync"
    requirement: "SPLT-01"
    verification:
      - kind: integration
        ref: "cmp on both files -> exit 0, 26 allowlist entries each side, measured 2026-09-06T14:23:47Z"
        status: pass
      - kind: integration
        ref: "npm run verify:disjoint -> exit 0 in BOTH repositories, byte-identical output, 0 violations"
        status: pass
    human_judgment: false
  - id: D8
    description: "All eight Phase 04.2 requirement statuses are set from cited, measured evidence, and PROD-06's withdrawn half is recorded as withdrawn rather than met"
    verification:
      - kind: integration
        ref: "every row of 04.2-VALIDATION.md's per-task map re-run in the repository it names -> all green; SC1 (typecheck+lint+test) green in both"
        status: pass
      - kind: other
        ref: "grep -E '^\\| (SPLT-0[1-4]|PROD-0[256]|QUAL-04) \\| Phase 04\\.2 \\|' .planning/REQUIREMENTS.md | wc -l -> 8"
        status: pass
      - kind: unit
        ref: "src/test/product-shell-reuse.test.tsx#rejects product-name literals (successor strength; the withdrawal block names all three successors)"
        status: pass
    human_judgment: false
  - id: D9
    description: "The ZERO-PAPER HUB planning-directory removal is a scheduled follow-up with a named owner, an explicit trigger and an executable command"
    verification:
      - kind: other
        ref: "04.2-DEFERRED-ITEMS.md#D37 — owner (repository owner via /gsd-quick), trigger (/gsd-verify-work 04.2 sign-off), exact git rm invocation and commit message, cross-referenced to EXCLUDED_PREFIXES in the auditor; mirrored in ROADMAP.md's post-verification item"
        status: pass
    human_judgment: false
  - id: D10
    description: "The split reads correctly to a person across both sites — card click-through, retired path with JavaScript disabled, the approved disclosure copy in place, the brochure from both paths, refresh, and the certificate in the address bar"
    verification: []
    human_judgment: true
    rationale: "Seven visual and interactive checks queued as task 3's <human-check>. A browser is the only place a rendered disclosure can be read the way a visitor reads it, and JavaScript-disabled behaviour is not observable from a fetch."
  - id: D11
    description: "The Kenya Data Protection Act 2019 sign-off (02-VALIDATION.md:91) remains OPEN and is not closed by the owner's approval of the visitor-facing copy"
    verification: []
    human_judgment: true
    rationale: "Carried forward deliberately unresolved. It requires someone with legal standing; the owner stated in terms at 04.2-04's gate that approving copy is not a data-protection review. Recorded so no later reader can infer closure from the approved disclosure."

duration: 28 min
completed: 2026-09-06
status: complete
---

# Phase 04.2 Plan 09: Close the Configuration Half, Finish Transport, Sync the Record Summary

**The three PostHog build variables now live on `KaruguDev/HAOO` and nowhere else, the HAOO domain serves over enforced HTTPS on both legs, the phase's planning record is a proven byte-for-byte superset in the HAOO repository, and all eight Phase 04.2 requirement statuses are set from measurements rather than intentions.**

## Performance

- **Duration:** 28 min
- **Started:** 2026-09-06T14:10:10Z
- **Completed:** 2026-09-06T14:38:40Z
- **Tasks:** 3/3
- **Files modified:** 6 in ZERO-PAPER HUB, 24 in HAOO, plus six GitHub repository-variable operations

## Test counts, before and after

| Repository | Before | After | Delta |
|---|---|---|---|
| `KaruguDev/HAOO` | 10 files / **680** tests | 10 files / **680** tests | **0** |
| `KaruguDev/ZERO-PAPERHUB` | 3 files / **32** tests | 3 files / **32** tests | **0** |

**The delta is zero in both and that is fully accounted for: this plan modified no executable source
in either repository.** Every change is a planning document, a GitHub repository-variable operation,
or a Pages setting read. `npm run typecheck && npm run lint && npm test` exits 0 in both;
`npm run verify:coverage` exits 0 in HAOO; `npm run verify:disjoint` exits 0 in both with
byte-identical output. Both working trees clean.

## Accomplishments

**The configuration-level half of the separation claim is closed.** The source-level half closed in
`04.2-06` and the artifact-level half in `04.2-07`; the three PostHog values were still on the wrong
repository until now. They were **copied, never retyped** — read out of the source repository as JSON
and written to the destination through stdin, so no value passed through a keyboard or an argument
vector — then compared byte-for-byte with `cmp` **before** the originals were deleted. A hand-typed
project key would have sent this site's events into a project the owner does not control.

**Transport was read before it was changed, and it was already done.** The certificate state and the
enforcement flag were read from the Pages API at `14:12:41Z`: `state = approved`,
`https_enforced = true`, `protected_domain_state = verified`, covering both legs and expiring
2026-12-02. The plan's contingency branch — record the state, retry, carry the item as owed — did not
fire, and nothing about the setting was changed. Live confirmation at `14:13:03Z`: `http` → `301` on
both legs, apex → `301` to `www`, `www` → `200`, certificate subject `CN = www.haoo.online`.

**The prior phase's gap evidence was re-taken on the hostname that ships.** `G-04.1-3` was closed on
2026-09-05 against `https://www.zero-paperhub.com/` — a host that no longer serves the HAOO document
at all. All four counts were taken again against `www.haoo.online` and appended to
`04.1-VERIFICATION.md` **without editing the original** (75 insertions, 0 deletions), so a reader can
see the gap was closed twice and why the second time was necessary.

**The frozen planning snapshot is reconciled.** `HAOO/.planning/` had been deliberately untouched for
the whole phase — a clone snapshot from `2065791`. One `rsync` pass, ZERO-PAPER HUB → HAOO, no
`--delete`, brought 24 paths across, and the destination is now a proven byte-for-byte superset over
all **239** files.

## Task Commits

| Task | Repo | Hash | Message |
|---|---|---|---|
| — (deviation) | ZPH | `a8eecd2` | `fix(04.2-09): correct the double-prefixed repository slug in this plan's verifications` |
| 1 | ZPH | `237cfda` | `feat(04.2-09): move the three PostHog build variables to HAOO and record the transport measurement` |
| 2 | ZPH | `7da9c5b` | `docs(04.2-09): re-take G-04.1-3 on www.haoo.online and schedule the planning-record removal` |
| 2 | HAOO | `b635338` | `docs(04.2-09): sync the phase 04.2 planning record from ZERO-PAPER HUB (D-03)` |
| 3 | ZPH | `9a103e1` | `docs(04.2-09): set all eight Phase 04.2 requirement statuses from measured evidence` |

**Close-out commits** (after the three task commits above):

| Repo | Hash | Message |
|---|---|---|
| ZPH | `9b50080` | `docs(04.2-09): complete the configuration, transport and record-reconciliation plan` (this summary) |
| ZPH | `7e45498` | `docs(04.2-09): record state, and revert the phase-complete overstatement` |
| ZPH | `7675f56` | `docs(04.2-09): record six plan-09 findings in the broken-windows ledger` |
| HAOO | `945f0b3` | `docs(04.2-09): sync the plan 09 close-out record from ZERO-PAPER HUB` |
| HAOO | `1c1c488` | `docs(04.2-09): sync the broken-windows ledger entries from ZERO-PAPER HUB` |

Both repositories pushed; both working trees clean; nothing unpushed. The four deploy runs triggered
during this plan — `34038506835` / `34038508658` (dispatched) and `34040265254` / `34040268747` (the
final pushes) — all concluded `success`. Live re-measured at 2026-09-06T14:46:27Z after the push
deploys: HAOO project chunk `/assets/haoo-DGFKBCjE.js` still carries the approved origin, the ZPH
chunk still carries **0**, transport still `approved` / `enforced`, HAOO root 200 and the retired path
200.


## The variable move, measured

| | Before, 14:11:00Z | After, 14:12:00Z |
|---|---|---|
| `KaruguDev/ZERO-PAPERHUB` | all three (created 2026-09-05 10:50:26Z / 10:50:50Z / 10:51:50Z) | **no variable at all**; `VITE_HAOO_` count **0** |
| `KaruguDev/HAOO` | **none** | exactly the three, as **variables** not secrets (created 14:11:47Z–14:11:49Z) |
| secrets | none | none — on either repository, so the report-credential absence check passes trivially on both sides |

```
cmp <source name/value JSON> <destination name/value JSON> -> exit 0

VITE_HAOO_MEASUREMENT_PROVIDER  len=7   sha256=67e5a3025bc013d0…  posthog
VITE_HAOO_POSTHOG_API_HOST      len=24  sha256=f388dc8f2e5eb447…  https://us.i.posthog.com
VITE_HAOO_POSTHOG_TOKEN         len=48  sha256=8b7e63ed79180031…  phc_… (public, write-only)
```

**Three created, four referenced — the counting correction stands.** `deploy.yml`'s Build step
assigns four browser-prefixed names. `VITE_HAOO_FORM_ENDPOINT` was deliberately **not** created: it
expands to `""` and `resolveQualifyEndpoint` falls back to
`https://formsubmit.co/ajax/info@haoo.online`, which is the intended production behaviour. The
correct wordings remain *"the three PostHog build variables"* or *"the four browser-prefixed
assignments"* — never a bare "three".

## Transport and TLS, measured with timestamps

| Measurement | Timestamp | Result |
|---|---|---|
| `gh api repos/KaruguDev/HAOO/pages` | **2026-09-06T14:12:41Z** | `cname=www.haoo.online`, `https_certificate.state=approved`, `https_certificate.domains=["www.haoo.online","haoo.online"]`, `expires_at=2026-12-02`, `https_enforced=true`, `protected_domain_state=verified` |
| `openssl s_client -servername www.haoo.online` | **2026-09-06T14:13:03Z** | `issuer = Let's Encrypt YR2`; `subject = CN = www.haoo.online`; `serial = 0609A5171B8224FD0D181CCBEC9CC50E7CC1`; `notBefore = Sep 3 07:14:32 2026 GMT`; `notAfter = Dec 2 07:14:31 2026 GMT`; `SAN = DNS:haoo.online, DNS:www.haoo.online` |
| apex certificate | 2026-09-06T14:13:03Z | **the same serial** — one certificate serves both legs |
| four HTTP/HTTPS probes | 2026-09-06T14:13:03Z | `http://www.haoo.online/` → 301 `https://www.haoo.online/`; `https://www.haoo.online/` → **200**; `http://haoo.online/` → 301 `https://www.haoo.online/`; `https://haoo.online/` → 301 `https://www.haoo.online/` |

**Nothing here was waiting on propagation.** Certificate issuance had already completed and
enforcement was already on when read, so nothing about transport is carried forward as owed. One
finding is carried forward, and it is about provenance rather than about enforcement — see D34 below.

## The bundle discriminator, absent then present

| Measurement | 2026-09-05, pre-variables (`04.2-02`) | 2026-09-06T14:16:37Z, post-variables |
|---|---|---|
| HAOO project chunk | `/assets/haoo-Di1q_TNO.js` | `/assets/haoo-DGFKBCjE.js`, 207,627 B, proved non-empty first |
| approved ingestion origin in it | **0 hits — ABSENT** | **PRESENT**, 2 occurrences |
| `phc_` project key in it | — | **1 occurrence**, inlined and world-readable by construction |
| vendor chunk | `/assets/posthog-sdk-DE6Cs-ON.js` | byte-identical, `sha256=fbdbe1335189d6bc…` — unchanged |
| ZPH artifact | — | `/assets/main-ClJKpN3o.js`, 179,611 B non-empty: **0** origins, **0** `phc_`, **0** `posthog`, **no** vendor chunk referenced |

The project chunk's content hash changed while the vendor chunk's did not — a second, independent
signal that the variables, and only the variables, changed the build.

**A counting-method note.** `04.2-02` recorded the vendor chunk at "1 hit" using `grep -c`, which
counts *lines*, and minified JavaScript is one line. Counting *occurrences* gives 2 for the same
byte-identical file. Nothing about the vendor chunk changed between the two measurements.

## The planning sync

Direction: `rsync -a "$ZPH_CHECKOUT/.planning/" -> "$HAOO_CHECKOUT/.planning/"`, executed
2026-09-06T14:23:13Z, re-run at 14:25:45Z after the later task-2 edits. `--delete` was deliberately
not used: zero paths existed only in HAOO, so it would have been a no-op, and a no-op flag that would
silently destroy an upstream-authored file the day one appears is not one to leave in a recorded
command.

| Before the sync | Count |
|---|---|
| paths present only in ZERO-PAPER HUB | **33** (7 summaries, the deferred-items file, `milestone.lock`, `v1-MILESTONE-AUDIT.md`, 3 phase dotfiles, 2 `.planner-contributions.md`, 18 gitignored research-cache files) |
| shared paths whose CONTENT differed | **7** (`REQUIREMENTS.md`, `ROADMAP.md`, `STATE.md`, `WINDOWS.md`, `04.2-SPLIT-CONTRACT.md`, `04.2-split-env.sh`, `04.2-09-PLAN.md`) |
| paths present only in HAOO | **0** — in either direction, before or after |

**Post-sync: 239 files compared with `cmp`, 0 mismatches, compared count printed and greater than
zero so the walk cannot have passed on an empty tree.** 24 paths were committed in HAOO (15 new,
9 updated, 0 deleted).

**The upstream restatement survived, asserted by content and not only by file identity.** Plan
`04.2-05` wrote the MEAS-08 cutover qualification into the ZERO-PAPER HUB copy of `REQUIREMENTS.md`
only, precisely so this sync would carry it forward. It was confirmed present in the **source before
the copy** (copying over a missing restatement would have erased it silently) and present in the
**destination after it** by content — `grep -c 'plan 04.2-05, the repository and domain split'` → 1,
with the sentence intact — and `diff` of the two files reports no difference. A file-identity check
alone would have passed on a sync that copied a reverted file.

**The two files that are byte-identical BY REQUIREMENT and NOT by sync (T-04.2-40) were untouched
and re-verified.** `shared-scaffold.txt` and `scripts/verify-tree-disjointness.mjs` live at each
repository root, outside `.planning/`, so the sync could not reach them. Re-measured
2026-09-06T14:23:47Z: `cmp` exit **0** on both, **26** allowlist entries each side, and
`npm run verify:disjoint` exit **0 in both repositories** with byte-identical output (102 paths
compared, 26 shared, 26 allowlisted, 0 violations, 3 ratified collisions none converged).

**The 04.1 coverage document was a no-op, verified rather than assumed:** `cmp` returned exit 0
*before* the sync ran, exactly as `04.2-05` recorded.

## Requirement statuses — all eight set from evidence

| ID | Status | Gate that actually passed |
|---|---|---|
| SPLT-01 | **Complete** | `npm run verify:disjoint` exit 0 in both, 0 violations |
| SPLT-02 | **Complete** | HAOO document + four brochure assets all 200; canonical/`og:url`/`og:image`/`twitter:image` all on `www.haoo.online`; retired path 200 with meta refresh, canonical, visible link, zero `<script>` |
| SPLT-03 | **Complete** | variables on HAOO only, ZPH empty, `cmp` before deletion, no secrets on either; origin present in the HAOO project chunk and absent from the ZPH artifact |
| SPLT-04 | **Complete** | the four approved strings measured present in the **deployed** artifact; `-t 'disclosure'` 20 cases green — **and this does not close D26** |
| PROD-02 | **Complete** | HAOO root 200; card target read from the deployed ZPH bundle (D23); recovery document 200 and scriptless; `products-section.test.tsx` 10 green |
| PROD-05 | **Complete** | full metadata set on the HAOO site's own document; `-t 'first-party HAOO root canonical and social metadata'` green |
| PROD-06 | **Complete at successor strength only** | `-t 'rejects product-name literals'` green; the reuse half **withdrawn**, naming three successors by exact title |
| QUAL-04 | **Complete** | direct navigation and refresh both 200; brochure 200 at 2,160,873 B; physical-file cases green; HTTPS enforced with a certificate naming the host |

Every row of `04.2-VALIDATION.md`'s per-task map was re-run in the repository its row names and every
one is green, plus `SC1` in both.

## Carried forward — genuinely owed, each with an owner and a phase

| Item | Status | Owner | Phase |
|---|---|---|---|
| **Kenya Data Protection Act 2019 sign-off** (`02-VALIDATION.md:91`) | **OPEN. NOT closed by owner copy approval.** | someone with legal standing | not yet scheduled |
| **Mail routing for `haoo.online`** — no MX; decision recorded 2026-09-05 to point at `mx1/mx2.privateemail.com` | **DECIDED, NOT EXECUTED** | repository owner | Phase 5 / LEAD-07 |
| **D34** — the certificate serving the HAOO host was issued 2026-09-03, two days before the reclaim | **OPEN, measured** | repository owner | Phase 5 or milestone audit |
| **D37** — removal of the ZERO-PAPER HUB planning directory | **SCHEDULED**, superset proven first | repository owner via `/gsd-quick` | on `/gsd-verify-work 04.2` sign-off |
| **D25** — the stranded browser record at the retired origin | **ACCEPTED by owner decision** | closed by decision | none |
| **Phase 04.1's live provider gates** — UAT 10 then UAT 8 | **OPEN** | privacy/product owner | Phase 04.1 UAT, before Phase 5 relies on any count |

### D26 is explicitly unresolved

The owner stated it in terms at plan `04.2-04`'s `blocking-human` gate:

> *"This approval covers visitor-facing wording ONLY. It does not close `02-VALIDATION.md:91`.
> Approving copy is not a data-protection review, and that item covers notice, storage, retention and
> processor decisions that need someone with legal standing to sign."*

**SPLT-04 reading `Complete` does not close it, and this summary says so where SPLT-04 is set, in
`REQUIREMENTS.md`'s evidence row, and in its carry-forward table.** SPLT-04 is a claim about whether
the visitor-facing statements are TRUE; the sign-off is a legal review of notice, storage, retention
and processor decisions. Neither implies the other. Nothing written by this plan reads as though
copy approval satisfied it.

## Decisions Made

- **The plan's own bundle verification was replaced, not weakened.** Its acceptance criterion says
  *project bundle*; its command greps a concatenation of every asset, which includes the vendor SDK
  chunk, and that chunk carries the ingestion origin as its own default regardless of the provider
  selector. The command therefore passes with zero contribution from the project chunk and would
  have passed identically on the 2026-09-05 pre-variables deploy. The stronger measurement the
  criterion actually asks for was taken instead (D35).
- **`04.2-02-PLAN.md`'s four occurrences of the same double-prefix defect were left unedited.** It is
  a completed plan's historical record; rewriting it would make its summary describe a document that
  no longer exists. Recorded in D36 instead.
- **The task-3 status-ceiling check was recorded as unsatisfiable rather than made to pass.** The
  only two routes to passing it are downgrading a true `Phase 1 | Complete` status this phase never
  touched, or deleting the sentence that records why four IDs moved off Phase 1 (D38).
- **Two roadmap goal lines carrying the pre-reversal apex host were corrected** to `www.haoo.online`
  with the reversal cited inline — a statement the owner's 2026-09-06 reversal made false, in the
  phase whose purpose is to leave the record truthful.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] The plan's own `gh` calls carried a double-prefixed repository slug**
- **Found during:** before task 1, as the prompt required
- **Issue:** `--repo "KaruguDev/$HAOO_REPO"` while `HAOO_REPO` is already `KaruguDev/HAOO`, expanding
  to `KaruguDev/KaruguDev/HAOO`. Every `gh` call using it would have failed.
- **Fix:** corrected to `"$HAOO_REPO"`. `04.2-DEFERRED-ITEMS.md` had surfaced lines 171 and 372 only;
  **173 and 190 carried the identical defect and were corrected too** — four occurrences, not two.
- **Files modified:** `.planning/phases/04.2-.../04.2-09-PLAN.md`
- **Verification:** all seven task-1 verifies and both remaining `gh`-based task-3 verifies pass
- **Committed in:** `a8eecd2`

**2. [Rule 1 - Bug] Task 1's bundle verification cannot fail as written**
- **Found during:** Task 1, running the `<verify>` block
- **Issue:** line 174 greps the concatenation of every referenced asset, which includes the vendor SDK
  chunk. That chunk carries the approved ingestion origin as its own default in every build. Measured:
  the vendor chunk alone satisfies the `grep -qF`, so the check passes with no contribution from the
  project chunk and would have passed on the pre-variables deploy.
- **Fix:** not weakened and not skipped — replaced with the per-chunk measurement the acceptance
  criterion actually specifies, giving a real absent-then-present pair on the project chunk.
- **Files modified:** none (a measurement correction, recorded as D35)
- **Verification:** project chunk fetched alone, proved non-empty, origin PRESENT; `04.2-02` measured
  the same position ABSENT
- **Committed in:** `237cfda`

**3. [Rule 2 - Missing Critical] The 2026-09-05 closure block in `04.1-USER-SETUP.md` had no pointer
   to its own supersession**
- **Found during:** Task 2
- **Issue:** the plan directs the re-taking into `04.1-VERIFICATION.md`, but the stale measurement
  table lives in `04.1-USER-SETUP.md`. A reader landing there would have concluded the site sends on
  the strength of three rows measured against a host that no longer carries the site.
- **Fix:** a superseding paragraph inserted immediately after the block, leaving every original byte
  intact (12 insertions, 0 deletions), pointing at the re-taken section.
- **Files modified:** `.planning/phases/04.1-.../04.1-USER-SETUP.md`
- **Verification:** `git diff --numstat` → `12 0`
- **Committed in:** `7da9c5b`

**4. [Rule 1 - Bug] Two roadmap lines still named the pre-reversal apex host**
- **Found during:** Task 3
- **Issue:** the phase entry and the phase Goal both read *"its own repository at `haoo.online`"*,
  which the owner's 2026-09-06 reversal made false — the apex now 301-redirects to the canonical
  `www` leg.
- **Fix:** corrected to `www.haoo.online` with the reversal cited inline rather than silently.
- **Files modified:** `.planning/ROADMAP.md`
- **Verification:** `grep -n 'haoo\.online' .planning/ROADMAP.md` — remaining hits are
  `manage.haoo.online` and `info@haoo.online`, which the reversal deliberately does not touch
- **Committed in:** `9a103e1`

---

**Total deviations:** 4 auto-fixed (2 bugs, 1 blocking, 1 missing critical)
**Impact on plan:** two of the four were defects in the plan's own checks rather than in either tree,
which is the class this phase has found nine times (D1, D6, D21–D24, D28, D31–D33). Neither was
worked around; both were recorded and the criterion measured directly instead. No scope creep — no
executable source in either repository was modified.

## Acceptance criteria that are unsatisfiable as written

**Three, all recorded rather than weakened.**

1. **Task 1's project-bundle check (`04.2-09-PLAN.md:174`) — non-discriminating.** Measured: passes
   from the vendor chunk alone. → **D35**. Replaced with the project-chunk measurement.
2. **Task 3's `Phase 1 | Complete` ceiling (`:369`) — measured 10, ceiling 8.** Nine are genuine
   traceability rows (`PROD-01`, `PROD-03`, `PROD-04`, `ONBD-01`…`ONBD-05`, `QUAL-06`) and one is the
   prose sentence recording that four IDs moved off Phase 1. Passing it requires falsifying a status
   or deleting that sentence. → **D38**. The criterion it stands in for — all eight Phase 04.2 IDs
   reading `Complete` — is satisfied and measured at 8 by the sibling check on line 368.
3. **Task 2's "all nine plans and all nine summaries" — not satisfiable at the instant task 2 runs.**
   Nine plans are present; the ninth summary is this document, written after the tasks finish. Closed
   at close-out by a second one-directional pass, recorded in the sync entry.

## Issues Encountered

**None that blocked.** Two things are worth recording:

- **`04.2-08-SUMMARY.md` contradicts itself** — its self-check says the allowlist has 19 entries and
  its readiness list says SPLT-01 is open and `verify:disjoint` is RED, while its own corrections
  section (written later, in the same file) ratifies 26 and marks SPLT-01 Complete. Measured: **26**
  entries both sides, `verify:disjoint` exit **0** both sides. The stale halves were true when
  written; the file is a historical record and is deliberately not rewritten. → **D40**.
- **`04.2-VALIDATION.md` has no per-task row for PROD-02**, one of the eight IDs this phase owns. Its
  status rests on direct measurement instead, stated as such in the requirement's own evidence row.
  → **D39**.

## Known Stubs

None. This plan created no code, no component and no data source.

## Threat Flags

None. No new network endpoint, auth path, file-access pattern or schema change at a trust boundary
was introduced. `T-04.2-42` through `T-04.2-47` were all mitigated as the register specified:
variables not secrets; values copied and `cmp`-compared before deletion; the reduced repository's
listing proved empty; transport read before enforcement; every status cited to the summary that took
its evidence; and every absence check gated on a non-empty fetch first. `T-04.2-SC` holds — no
package operation occurred.

**One finding worth a security reader's attention, and it is not a compromise.** The certificate
serving `www.haoo.online` was issued **2026-09-03T07:14:32Z**, two days before the 2026-09-05
reclaim, while a third party held the Pages claim; `04.2-SPLIT-CONTRACT.md` recorded that certificate
as un-revoked, and the measurement shows it is the one currently being served on both legs. GitHub
Pages terminates TLS itself and never releases a private key to a site owner, so the key was in
GitHub's custody throughout and no third party can impersonate the host. It is carried forward as
**D34** because it is the certificate a CT-log monitor reads until 2026-12-02, and forcing a reissue
costs a brief TLS outage — an owner's decision, not an executor's.

## User Setup Required

**None newly created by this plan.** The `user_setup` block anticipated an authentication gate on the
variable operations; none occurred. The existing `gh` token (`gist, read:org, repo`) was sufficient
for variable reads, writes and deletes, for the Pages API read, and for dispatching both workflows.
**No `.github/workflows/*` file was written or transmitted**, so the absent `workflow` scope was
never needed.

`04.1-USER-SETUP.md` keeps `Status: Incomplete` — its remaining gates are the live provider
checkpoints, which this plan does not touch.

## Next Phase Readiness

**All nine Phase 04.2 plans are executed and all eight requirement statuses are set from
measurements. The phase is NOT yet marked complete, and that is deliberate:** the roadmap's
execution-order row reads `9/9 | In Progress`, matching the convention every unverified phase in this
project follows (Phases 2, 3, 4 and 04.1 all read `In Progress` at full plan counts; only Phase 1,
which has a verification, reads `Complete`). An earlier draft of this plan wrote `Complete` with a
date into that row and `[x]` against the phase; both were **reverted** as overstatements —
`/gsd-verify-work 04.2` has not run. Four things it and Phase 5 must inherit:

1. **`/gsd-verify-work 04.2` sign-off is the trigger for D37.** The removal of the ZERO-PAPER HUB
   planning directory is scheduled with its owner, trigger and exact command, and its superset
   precondition is already proven. Do not run it before the verifier has read the record.
2. **D26 is OPEN and must not be inherited as closed.** The visitor-facing copy is approved; the
   Kenya DPA 2019 sign-off is a separate item needing someone with legal standing. `SPLT-04` reading
   `Complete` is a claim about the truth of the statements, not a data-protection review.
3. **Mail routing is decided but not executed.** `haoo.online` still has no MX records, and
   `info@haoo.online` is the qualification-form fallback target, the `mailto:` onboarding link and a
   `noscript` recovery link. **Phase 5's success criterion 4 depends on it** and cannot pass until
   the DNS change is made.
4. **Phase 04.1's live provider gates are still open.** The deployment half of `G-04.1-3` is closed
   against the hostname that ships; the provider's half — whether PostHog Cloud US accepts the
   reduced payload and honours `$process_person_profile: false` — is untouched. **Zero events
   arriving at UAT checkpoint 10 is a FAILURE, never a quiet pass**, and checkpoint 8 must follow it
   rather than precede it.

The end-of-phase human walkthrough (seven checks across both sites) is queued as coverage item D10.

---
*Phase: 04.2-split-haoo-into-its-own-repository-and-domain*
*Completed: 2026-09-06*

## Self-Check: PASSED

- All six modified/created ZERO-PAPER HUB planning files verified present with `[ -f ]`; the two
  key HAOO destination files verified present in that checkout.
- All five ZERO-PAPER HUB commits (`a8eecd2`, `237cfda`, `7da9c5b`, `9a103e1`, `d52c892`) and the
  one HAOO commit (`b635338`) verified in `git log --oneline --all` in their own repositories.
- Every append verified additions-only with `git diff --numstat`: `04.1-VERIFICATION.md` 75/0,
  `04.1-USER-SETUP.md` 12/0, `04.2-DEFERRED-ITEMS.md` 424/0.
- `npm run typecheck && npm run lint && npm test` exit 0 in both repositories, counts unchanged at
  10 files / 680 tests (HAOO) and 3 files / 32 tests (ZPH).
- `npm run verify:disjoint` exit 0 in both with byte-identical output; `npm run verify:coverage`
  exit 0 in HAOO after the sync.
- The 239-file superset comparison re-run after the final sync: 0 mismatches.
- Both deploy runs verified `success` (`34038506835` HAOO, `34038508658` ZPH); both working trees
  clean; nothing unpushed in either repository.
