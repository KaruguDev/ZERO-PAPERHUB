---
phase: 04.2-split-haoo-into-its-own-repository-and-domain
verified: 2026-09-06T19:40:13Z
status: human_needed
score: 6/7 must-haves verified
behavior_unverified: 1
overrides_applied: 0
re_verification:
  previous_status: null
  note: "Initial verification. No prior 04.2-VERIFICATION.md existed."
behavior_unverified_items:
  - truth: "HAOO is MEASURED from its own repository — a visitor action on www.haoo.online becomes a count in the HAOO-owned PostHog project"
    test: "Load https://www.haoo.online/ in a real browser with no ad-blocker, perform two or three allowlisted actions (open the brochure, expand a section, submit nothing), wait for the PostHog ingestion delay, then re-run the owner report from the HAOO checkout."
    expected: "At least one non-zero count appears. If every count is still zero after a known-good browser session, the ingestion path is broken and SPLT-03's configuration success is masking a dead pipeline."
    why_human: "The report currently renders every count as zero (9x 'No recorded actions', the only rendered numeral is 0). Zero is consistent with a capturing deploy that went live hours earlier, but it is byte-for-byte identical to what a rejected-ingestion path produces. No test and no static check can tell the two apart; only a deliberate browser session followed by a re-run can. Correctly NOT claimed as proven anywhere in the record (04.2-UAT.md G-04.2-9 limit (a); 04.1 D-05)."
warnings:
  - id: W-1
    severity: warning
    title: "D37's superset precondition is stale, and the follow-up as written would destroy three artifacts"
    detail: >
      ROADMAP.md (post-verification follow-up) and 04.2-DEFERRED-ITEMS.md D37 both cite a
      2026-09-06T14:23:23Z measurement (239 planning files, 0 mismatches) as THE precondition
      for removing the ZERO-PAPER HUB .planning/ directory, and neither requires that the
      measurement be re-taken. Re-measured at verification time: HAOO is NO LONGER a superset.
      Two planning files are ZPH-only (04.2-UAT.md, COVERAGE.md) and this VERIFICATION.md makes
      three. The 222 shared files remain 0-mismatch. The follow-up's trigger is verification
      sign-off — precisely the event that guarantees the cited precondition is stale — so acting
      on it as written deletes the UAT record, the API-surface matrix and this report with no
      copy in the destination.
    required_correction: "Amend D37 and the ROADMAP follow-up so the superset walk is RE-RUN immediately before removal, not cited from 14:23:23Z. The removal must not proceed on the stale figure."
  - id: W-2
    severity: warning
    title: "The G-04.2-9 gap fix is committed but UNPUSHED in both repositories"
    detail: >
      HAOO 5dfffe8 and ZERO-PAPER HUB acb8c23, ce14e6c, 3b08e3d, 1f262f2 are local-only
      (verified against origin/main after git fetch). Consequence on the PUBLISHED repositories:
      KaruguDev/HAOO main still carries the measured-FALSE REPORT_CAVEATS sentence "The provider
      echoes the query this report submitted", the report still fails closed on every run, and
      the 04.1 COVERAGE.md correction of the two falsified Query API rows is absent from both
      published trees. Mitigating, and why this is a warning rather than a blocker: src/reporting/
      is a local CLI, not part of the browser bundle (0 occurrences of the caveat or of any
      reporting symbol in the deployed chunk), so no visitor-facing artifact is affected, and no
      Phase 04.2 requirement status rests on the unpushed work — every SPLT/PROD/QUAL evidence
      row was re-measured live and holds.
    required_correction: "Push both repositories before the phase is treated as shipped."
carry_forward:
  - item: "Kenya Data Protection Act 2019 sign-off (02-VALIDATION.md:91)"
    status: "OPEN — correctly recorded, not closed by this phase"
    note: "Verified independently: 02-VALIDATION.md still carries the row unclosed; nothing in either repository asserts closure; non-implication is stated in the owner's own words at REQUIREMENTS.md:249 and re-stated inside the SPLT-04 evidence row at :229. SPLT-04 and this sign-off are different claims and the record makes neither imply the other."
    owner: "someone with legal standing, engaged by the repository owner"
  - item: "haoo.online has NO MX records (re-measured at verification time: dig +short MX haoo.online returns nothing)"
    status: "OPEN — decided 2026-09-05, deliberately not executed in this phase"
    note: "info@haoo.online is the qualification-form fallback, the mailto: onboarding link and a noscript recovery target. With no MX, RFC 5321 implicit-MX falls back to the A record, which is GitHub Pages, which runs no SMTP. Correctly scoped out (04.2-SPLIT-CONTRACT.md § Mail routing; no plan in this phase performs or asserts it) and recorded as a Phase 5 blocker at STATE.md:230. Surfaced here because the phase goal invokes the journey Phase 5 will prove, and that journey currently ends at an unreachable inbox."
    owner: "repository owner — Phase 5 / LEAD-07"
human_verification:
  - test: "Perform a real browser session on https://www.haoo.online/ (no ad-blocker), trigger two or three allowlisted actions, wait out the ingestion delay, then re-run the owner report."
    expected: "At least one non-zero count. Persistent all-zero after a known-good session means the ingestion path is broken."
    why_human: "Every count in the generated report is zero. Zero is indistinguishable from a broken ingestion path by any static or test-based means."
  - test: "Before removing the ZERO-PAPER HUB .planning/ directory, re-run the D37 superset walk and confirm 0 mismatches AND 0 ZPH-only files."
    expected: "The walk must be re-taken, not cited from 14:23:23Z. It currently reports two ZPH-only files, three after this report is written."
    why_human: "The recorded precondition is stale by construction and the follow-up is destructive."
  - test: "Push HAOO 5dfffe8 and ZERO-PAPER HUB acb8c23..1f262f2 to origin, then confirm both Pages deployments go green."
    expected: "The published repositories carry the corrected provenance caveat and the corrected 04.1 coverage rows."
    why_human: "The verifier does not commit or push. The published trees currently carry a sentence the provider's own response contradicts."
  - test: "Add MX records for haoo.online at mx1/mx2.privateemail.com and send one test message to info@haoo.online."
    expected: "The message arrives. Until it does, the deployed qualification journey has no reachable destination."
    why_human: "DNS change plus a live mail send; out of this phase's scope by recorded decision, owed by Phase 5."
---

# Phase 04.2: Split HAOO into its Own Repository and Domain — Verification Report

**Phase Goal:** As the HAOO product owner, I want HAOO to build, deploy, and be measured from its own repository at `www.haoo.online` (the apex `haoo.online` 301-redirects to it; decision (a) reversed 2026-09-06), so that the deployed journey Phase 5 proves is the one that ships permanently rather than an arrangement that is about to change.

**Verified:** 2026-09-06T19:40:13Z
**Status:** human_needed
**Re-verification:** No — initial verification

Every figure below was re-measured by this verifier against the two working trees, the two GitHub
repositories and the two live sites. No claim is carried over from a SUMMARY.md on its own authority.

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | **SC1** — Each repository builds, typechecks, lints, tests and deploys standalone with no source of the other, and neither suite reads a file the other owns | ✓ VERIFIED | HAOO `npm test` (build-first): **10 files, 683 passed**. ZPH `npm test`: **3 files, 32 passed**; `npm run typecheck` and `npm run lint` both clean. `npm run verify:disjoint` exit **0 in both**, byte-identical output: 102 paths compared, 26 shared, 26 allowlisted, **0 violations**, 3 ratified collisions (0 converged), 0 ZPH product sources shipping HAOO source, 0 HAOO files naming a home-page symbol. `scripts/verify-tree-disjointness.mjs` and `shared-scaffold.txt` confirmed **byte-identical** across repos by `cmp`. Both repos' own Pages workflows show recent **successful** runs (HAOO 34040368611, ZPH 34040365363). Suite-isolation checked directly: the empty-tree refusal test imports the auditor from its **own** ROOT and drives it with synthetic file lists and a `mkdtemp` fixture — it does not read the sibling checkout; `verify:disjoint` is a separate npm script, not part of `npm test`. |
| 2 | **SC2** — Every published HAOO URL resolves on the HAOO domain, and the ZPH Products card links to it | ✓ VERIFIED | `index.html`: `canonical` and `og:url` → `https://www.haoo.online/`; `og:image` and `twitter:image` → `https://www.haoo.online/brochure/brochure-preview.png`; `rel="alternate" type="application/pdf"` → `/brochure/HAOO-Marketing-Brochure.pdf`. `CNAME` = `www.haoo.online`. Live: brochure PDF **200** (`content-length: 2160873`), preview **200** (637,261 B). Card link verified in the **deployed** ZPH bundle `assets/main-ClJKpN3o.js` (179,611 B): it carries `https://www.haoo.online/` and the preview URL and **no other** `haoo.online` URL. Source side: `src/products/registry.ts` holds an inline `ProductCard` with a locally declared type and **no import** from any product-definition module. |
| 3 | **SC3** — Direct navigation and refresh work on the HAOO host, and the retired path still reaches the HAOO page | ✓ VERIFIED | Live, re-measured: `https://www.haoo.online/` → **200** (4,147 B); brochure → **200**; `http://haoo.online/` → **301** → `https://www.haoo.online/`; `https://haoo.online/` → **301** → same. Retired path `https://www.zero-paperhub.com/products/haoo/` → **200**, 4,353 B, **0 `<script>` tags**, carrying `<meta http-equiv="refresh" content="0; url=https://www.haoo.online/">`, `rel="canonical"` to the same, and two plain anchors (the page and the brochure at its new root-level URL). Both documents are physical files — the ZPH one is `public/products/haoo/index.html`, copied verbatim, with no build input and no rewrite rule (`public/.htaccess` and `public/CNAME.txt` confirmed **absent**). |
| 4 | **SC4** — Measurement variables, ingestion host and report credentials are configured in the HAOO repository only; a ZPH build carries no measurement code, ingestion origin or credential shape | ✓ VERIFIED | `gh variable list --repo KaruguDev/HAOO` → exactly the three `VITE_HAOO_*` names, as **variables**. `--repo KaruguDev/ZERO-PAPERHUB` → **empty**. `gh secret list` → **empty on both**. Deployed **ZPH** chunk (179,611 B, proved non-empty): **0** case-insensitive `posthog`, **0** `us.i.posthog.com`, **0** `phc_`. Deployed **HAOO** chunk `assets/haoo-DGFKBCjE.js` (207,627 B): ingestion origin present (×2), project token present (×1). Source/config side: ZPH `package.json` and `package-lock.json` carry no analytics SDK; ZPH `src/vite-env.d.ts` declares no `VITE_*` key; ZPH `deploy.yml` declares **no** `VITE_` variable at all (HAOO's declares four). |
| 5 | **SC5** — The parent-relationship line, the measurement disclosure and the qualification subject/source are true of a standalone HAOO domain | ✓ VERIFIED | `src/products/types.ts:196-197` makes `controllerHeading` and `controllerNote` **required** (`readonly … : string`, not optional), so a forgotten controller statement is a typecheck failure. `MeasurementDisclosure.tsx` contains **0** copy literals — every sentence is read from product data. All four sentences confirmed present, once each, in the **deployed** HAOO chunk: the controller heading, the "does not change who operates it or who receives what you send" note, the PostHog processor sentence, and "A ZERO-PAPER HUB product". `sourceNote` (`src/products/haoo.ts:635`) names `www.haoo.online`. Storage key renamed to `haoo.ctx.v2` with `schemaVersion: 2`. No parent-host literal in any buildable or published HAOO source — the only three files naming `zero-paperhub.com` are `AGENTS.md`, the disjointness auditor (which must name both trees) and the allowlist. |
| 6 | **SC6** — Every Phase 3 / 04.1 privacy and boundary invariant is asserted by HAOO's own suite at the same strength, and anything that could not survive is withdrawn with a named successor in the same commit | ✓ VERIFIED | **PROD-06** withdrawal read in full at `src/test/product-shell-reuse.test.tsx:273-293`; all **three** successors confirmed present **by exact title** at lines 201, 230 and 311, and green in the 683-test run. **`validateEchoedQuery`** withdrawn with `resolveQueryProvenance` named, the name deliberately not reused, byte-exact echo equality retained and the widening limited to `null`/`undefined` (HAOO 5dfffe8). **`productRoute`** withdrawn in `registry.ts` with the `href` field named as successor and the name deliberately not reused. 40 out-of-band entries in `04.2-DEFERRED-ITEMS.md`. Debt-marker scan (`TBD`/`FIXME`/`XXX`) over every tracked non-planning file in **both** repositories: **0 matches**. |
| 7 | **Goal clause "be MEASURED from its own repository"** — a visitor action on the HAOO domain becomes a count in the HAOO-owned project | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | The configuration and artifact halves are fully verified (truth 4). The end-to-end half is not. The generated report (`HAOO/.reports/haoo-funnel-report.html`, 28,272 B) renders correctly — cutover header present, all four periods bounded, all-time reads "No recorded actions in this period" rather than a project-since count — but **every count is zero**: the only numeral rendered anywhere is `0`, and "No recorded actions" appears **9 times**. This proves the report can QUERY and RENDER. It does **not** prove ingestion is accepted. See Human Verification. |

**Score:** 6/7 truths verified (1 present, behavior-unverified)

### Requirements Coverage

All eight requirement IDs named for this phase are accounted for. `grep -E "^\| *[A-Z]+-[0-9]+ *\| *Phase 04\.2"` over REQUIREMENTS.md returns **exactly these eight rows and no others** — there are **no ORPHANED requirements**.

| Requirement | Source Plan(s) | Status in REQUIREMENTS.md | Verifier finding | Evidence |
|---|---|---|---|---|
| **SPLT-01** | 01, 02, 03, 06, 08, 09 | Complete | ✓ SATISFIED | Re-ran `verify:disjoint` in both repos — every figure in the recorded evidence row reproduced exactly (102/26/26/0, 3 collisions 0 converged). Scripts and allowlist byte-identical. |
| **SPLT-02** | 01, 02, 03, 07, 09 | Complete | ✓ SATISFIED | Every URL re-fetched. All resolve on the HAOO domain; retired path recovers scriptlessly. |
| **SPLT-03** | 05, 06, 07, 09 | Complete | ✓ SATISFIED **as written** | The requirement text is a **configuration-and-artifact** claim ("configured in the HAOO repository only, and a ZPH build carries no measurement code, ingestion origin, or credential shape") and every clause of it is independently confirmed. It is **not** a live-ingestion claim, and the record does not stretch it into one. The goal's broader "be measured" clause is tracked separately as truth 7. |
| **SPLT-04** | 04, 09 | Complete | ✓ SATISFIED | Copy verified in the deployed artifact, controller members required at the type level, component free of literals. Does **not** close the DPA sign-off — see below. |
| **PROD-02** | 01, 07, 09 | Complete | ✓ SATISFIED | HAOO root 200, ZPH card target read from the deployed bundle, retired path recovers. Amended text names the meta-refresh mechanism honestly and states the four assets are **not** retained. |
| **PROD-05** | 01, 02, 03, 09 | Complete | ✓ SATISFIED | Title, description, canonical and the full OG/Twitter set present on HAOO's **own** document, all naming `www.haoo.online`. |
| **PROD-06** | 01, 02, 06, 09 | Complete **at successor strength only** | ✓ SATISFIED — properly retired, **not** quietly weakened | See the dedicated finding below. |
| **QUAL-04** | 01, 02, 07, 09 | Complete | ✓ SATISFIED | Direct navigation and refresh both 200; brochure served as a physical file. Pages API re-read live: `https_enforced: true`, cert state `approved`, `protected_domain_state: verified`. Live certificate re-read: `CN = www.haoo.online`, SAN `DNS:haoo.online, DNS:www.haoo.online`, valid to **Dec 2 2026**. The four retired-path asset URLs return **404** exactly as the amended requirement's own text says they will. |

### Key Link Verification

| From | To | Via | Status | Details |
|---|---|---|---|---|
| ZPH `registry.ts` | HAOO domain | build-time absolute literal | ✓ WIRED | `href: 'https://www.haoo.online/'`. No import from any HAOO-owned module — D-06's coupling is genuinely severed, and the accepted drift cost is named in the file's own header rather than mitigated. |
| ZPH card link | deployed artifact | Vite build | ✓ FLOWING | The literal is present in the live `main-ClJKpN3o.js`, so the link a visitor clicks is the one in source. |
| `CNAME` literal | canonical / og:url / twitter:image | build-output assertions | ✓ WIRED | All four name `www.haoo.online`; `CNAME` pins the same host. |
| Cutover constant | both SQL builders | `cutoverBound(HAOO_DOMAIN_CUTOVER_DAY)` | ✓ WIRED | Applied at `generate.ts:268` (bounded ranges) **and** `:301` (the all-time branch — the one where mixing would actually occur). Neither branch is unbounded. |
| Required type members | typecheck | `readonly controllerHeading/controllerNote: string` | ✓ WIRED | Non-optional, so omission is a build failure, not a blank region. |
| `shared-scaffold.txt` | disjointness check meaning | closed 26-entry allowlist, byte-identical in both repos | ✓ WIRED | Owner-ratified on two grounds; 26 subtracted, 0 violations. |
| Non-empty guard | check honesty | `auditSharedPaths` refuses an empty or fully-excluded tree | ✓ WIRED | Refusal proved by test in **both** repos, including the `.planning`-only case where a tree is non-empty by length and empty by subject. |
| HAOO deploy workflow | ingestion origin | `vars.VITE_HAOO_POSTHOG_API_HOST` | ✓ WIRED at build time; ⚠️ see truth 7 for the runtime half | The origin is demonstrably inlined in the deployed chunk. Whether events reach the project is a different question and is not established. |

### Data-Flow Trace (Level 4)

| Artifact | Value | Source | Produces real data | Status |
|---|---|---|---|---|
| ZPH `ProductsSection` | card name, relationship, outcome, href, preview | inline `HAOO_CARD` literal | Yes — deliberately a literal under D-06 | ✓ FLOWING |
| HAOO `MeasurementDisclosure` | every disclosure sentence | product definition, 0 component literals | Yes — confirmed in the deployed chunk | ✓ FLOWING |
| ZPH retired-path document | recovery target | static file, verbatim copy | Yes — 200 live, 0 scripts | ✓ FLOWING |
| HAOO owner report | period counts | PostHog Query API | **Renders, but every value is 0** | ⚠️ STATIC-INDISTINGUISHABLE — the query and render path flow; whether the data source has data is unestablished |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|---|---|---|---|
| HAOO suite passes build-first | `npm test` (HAOO) | 10 files, 683 passed | ✓ PASS |
| ZPH suite passes build-first | `npm test` (ZPH) | 3 files, 32 passed | ✓ PASS |
| ZPH typechecks and lints | `npm run typecheck`, `npm run lint` | clean | ✓ PASS |
| Disjointness holds both directions | `npm run verify:disjoint` in each repo | exit 0, 0 violations, identical output | ✓ PASS |
| Auditor and allowlist identical across repos | `cmp` ×2 | identical | ✓ PASS |
| Apex redirects to www | `curl -sI http://haoo.online/`, `https://haoo.online/` | 301 → `https://www.haoo.online/` (both) | ✓ PASS |
| Retired path recovers scriptlessly | `curl https://www.zero-paperhub.com/products/haoo/` | 200, 4,353 B, 0 `<script>`, 0s meta refresh + canonical + 2 anchors | ✓ PASS |
| Retired assets 404 as the requirement states | `curl` ×2 | 404, 404 | ✓ PASS |
| ZPH deployed bundle carries no measurement | fetch + grep | 0 posthog / 0 origin / 0 `phc_` in 179,611 B | ✓ PASS |
| HAOO deployed bundle carries the origin | fetch + grep | origin ×2, token ×1 in 207,627 B | ✓ PASS |
| Disclosure copy live on the deployed artifact | grep ×4 | all four sentences, once each | ✓ PASS |
| Variables in HAOO only, no secrets anywhere | `gh variable list` ×2, `gh secret list` ×2 | 3 / 0 / 0 / 0 | ✓ PASS |
| HTTPS enforced and certificate valid | `gh api …/pages`, `openssl s_client` | enforced, approved, verified; CN + both SANs, to 2026-12-02 | ✓ PASS |
| Domain verification survives a future unlink | `dig TXT _github-pages-challenge-karugudev.<domain>` ×2 | both resolve | ✓ PASS |
| PROD-06 successors exist by exact title | grep ×3 | lines 201, 230, 311 | ✓ PASS |
| Owner report actually generates | inspect `.reports/haoo-funnel-report.html` | 28,272 B, header + all four periods render | ✓ PASS (render only) |
| Owner report returns real counts | grep for numerals | only `0`; "No recorded actions" ×9 | **? SKIP → human** |
| `info@haoo.online` can receive mail | `dig +short MX haoo.online` | **no MX** | **? SKIP → Phase 5** |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|---|---|---|---|---|
| — | — | `TBD` / `FIXME` / `XXX` across every tracked non-planning file in both repositories | — | **0 matches.** No unreferenced debt markers. |

Withdrawals in this phase are recorded in place with named successors rather than deleted, which is
the opposite of the usual anti-pattern and is worth stating explicitly: `productRoute`,
`validateEchoedQuery` and the PROD-06 reuse case are each retired with the successor named, the old
name deliberately **not** reused, and the reason a shrunken form would have been worse than absence
spelled out.

---

## The Four Questions Asked of This Verification

### 1. Is SPLT-03's "measured from its own repository" genuinely achieved, given every count is zero?

**Split the claim, because the record does and the split is the honest one.**

**SPLT-03 as written is achieved.** Its text is a claim about *where measurement is configured* and
*what the other build does not carry*. Every clause is independently confirmed above: three variables
on HAOO and none on ZPH, no secrets on either, the ingestion origin inlined in the deployed HAOO
chunk and wholly absent from the deployed ZPH chunk, no SDK in the ZPH dependency tree, no `VITE_`
variable in the ZPH workflow. ROADMAP SC4 is scoped identically. Nothing here is inferred.

**The goal's broader "be measured" clause is NOT established, and I am recording it as unproven.**
The report renders every count as zero. I re-generated nothing — I read the artifact the fix produced
and confirmed the only numeral in 28,272 bytes is `0`, with "No recorded actions" nine times over. As
the phase's own UAT says in as many words, that is consistent with a capturing deploy that went live
hours earlier and *also* exactly what a broken ingestion path looks like, and nothing available to
either a test or a static check can separate the two.

**The record does not overstate this.** `04.2-UAT.md` limit (a) states it plainly, names 04.1 D-05 as
where live ingestion acceptance stays an owner observation, and does not claim otherwise anywhere
else I could find. **Live ingestion acceptance is NOT recorded as proven by this verification.** It is
truth 7, marked PRESENT_BEHAVIOR_UNVERIFIED, and it is the reason this phase is `human_needed`
rather than `passed`.

### 2. Is PROD-06's withdrawn half properly retired with reachable named successors?

**Yes — properly retired, and not weakened. This is the strongest-handled item in the phase.**

- The withdrawal is **recorded in the code it removed**, at `src/test/product-shell-reuse.test.tsx:273-293`,
  not only in prose.
- It names **three** successors by exact title. I confirmed all three exist at those exact titles
  (lines 201, 230, 311) and pass inside the 683-test run — they are reachable, not aspirational.
- The stated reason is the correct one and is not a convenience: the claim did not become false, it
  became **vacuous**. Iterating a one-element collection cannot distinguish a generic shell from one
  hardcoded to its single element, so the case would have gone on passing while proving nothing.
  Retiring it beats keeping a shrunken version.
- The first successor **reconstructs the substance** of the withdrawn claim without a registry: it
  renders a synthetic second product (`ZENITH`, defined in the test file and deliberately not the
  shipped one) through every product-named shell surface and asserts `container.textContent` carries
  no `HAOO` anywhere. That is the original claim's real content — the shell works for a product it
  has never seen — recovered rather than surrendered.
- `REQUIREMENTS.md:232` marks PROD-06 **"Complete at its successor strength only"** and states in the
  same row that the withdrawn half **is NOT recorded as met**. A later reader cannot inherit the
  reuse claim from the Complete marker.

Judgment: this is a retirement that ended up *stronger* than the thing retired. Nothing was quietly
weakened.

### 3. Is SPLT-04 Complete consistent with the Kenya DPA 2019 sign-off remaining OPEN?

**Yes. They are different claims, and I could not find anywhere the record lets either imply the other.**

SPLT-04 claims the visitor-facing relationship, controller and disclosure **statements are true of a
standalone HAOO domain**. That is a factual claim about sentences, verified against the deployed
artifact. The DPA sign-off is a **legal review** of notice, storage, retention and processor
decisions. One does not entail the other in either direction.

Independently confirmed:

- `02-VALIDATION.md` still carries the row unclosed ("Privacy/legal sign-off on the D-25 collection
  disclosure and Kenya DPA 2019 posture … Privacy/legal owner approves … before production
  collection").
- `REQUIREMENTS.md:249` carries it as **OPEN** with the owner named ("someone with legal standing")
  and scheduling stated as *"not yet scheduled — must not be inherited as closed by Phase 5"*.
- The owner's own words are quoted at the same line: *"This approval covers visitor-facing wording
  ONLY. It does not close 02-VALIDATION.md:91. Approving copy is not a data-protection review."*
- `REQUIREMENTS.md:229` — the SPLT-04 **Complete** row itself — ends with **"This does NOT close the
  Kenya DPA 2019 sign-off."** The disclaimer is inside the row that could otherwise be misread, which
  is where it does the most good.
- `04.2-DEFERRED-ITEMS.md` D26 records it still open. Nothing in either repository asserts closure.

Three independent recordings plus an in-row disclaimer. Correct.

### 4. Anything in the record that overstates what was actually established?

Two things — neither of them a claim about the phase deliverable, both of them consequential.

**W-1 — D37's superset precondition is cited in the past tense as if it still stands, and it does not.**
Both `ROADMAP.md` and `04.2-DEFERRED-ITEMS.md` D37 present a 2026-09-06T14:23:23Z walk (239 files, 0
mismatches) as **the** precondition for deleting the ZERO-PAPER HUB `.planning/` directory, and
neither requires it be re-taken. I re-measured: the 222 shared files are still 0-mismatch, but HAOO
is **no longer a superset** — `04.2-UAT.md` and `COVERAGE.md` exist only in ZPH, and this report makes
three. The follow-up's trigger is *verification sign-off*, which is precisely the event that
guarantees the cited figure is stale. Executed as written, it destroys the UAT record, the API-surface
matrix and this verification report. The record must be amended to require a fresh walk immediately
before removal.

**W-2 — the G-04.2-9 fix is committed but unpushed in both repositories.** HAOO `5dfffe8` and ZPH
`acb8c23`, `ce14e6c`, `3b08e3d`, `1f262f2` are local-only. So on the **published** `KaruguDev/HAOO`
main, `REPORT_CAVEATS` still contains the sentence measured false — *"The provider echoes the query
this report submitted"* — the report still fails closed on every run, and the corrected 04.1 Query API
rows are absent from both published trees. The fix commit's own message says the false statements were
"removed rather than left standing"; on the published artifact they are still standing. This is a
warning and not a blocker for two reasons I verified rather than assumed: `src/reporting/` is a local
CLI with **zero** presence in the deployed browser chunk, and **no** Phase 04.2 requirement status
depends on it — every SPLT/PROD/QUAL evidence row was re-measured live and holds without it.

**What did NOT overstate, and deserves saying.** I spot-checked the recorded figures against fresh
measurements and they reproduce exactly, not approximately: 102/26/26/0 violations; deployed chunk
sizes 207,627 and 179,611 bytes; HAOO root 4,147 bytes; retired document 4,353 bytes; brochure
2,160,873 bytes; certificate CN and both SANs; `https_enforced`, cert state and
`protected_domain_state`. The phase-level `[x]` and the "Complete | 2026-09-06" execution-order value
were **reverted** by the executor as an overstatement before verification ran, and `STATE.md` reads
"ALL PLANS EXECUTED, AWAITING VERIFICATION". The UAT's own gap entry volunteers both limits of the
report run rather than burying them. The record's honesty is above the level this verification
assumed going in.

---

### Human Verification Required

**1. Confirm the measurement pipeline actually ingests**
**Test:** Load `https://www.haoo.online/` in a real browser with no ad-blocker. Trigger two or three
allowlisted actions (open the brochure, expand a section). Wait out the PostHog ingestion delay, then
re-run the owner report from the HAOO checkout.
**Expected:** At least one non-zero count. If every count is still zero after a known-good session,
the ingestion path is broken and SPLT-03's configuration success is masking a dead pipeline.
**Why human:** Zero counts are byte-for-byte identical whether the cause is a young deploy or a
rejected ingestion path. No static check or test can separate them.

**2. Re-measure the D37 superset before deleting the ZPH planning directory** *(W-1)*
**Test:** Immediately before removal, re-run the superset walk and require **0 mismatches AND 0
ZPH-only paths**.
**Expected:** It currently reports two ZPH-only files, three once this report is written. Amend D37
and the ROADMAP follow-up so the walk is re-run rather than cited from 14:23:23Z.
**Why human:** The recorded precondition is stale by construction and the follow-up is destructive.

**3. Push both repositories** *(W-2)*
**Test:** Push HAOO `5dfffe8` and ZPH `acb8c23..1f262f2`; confirm both Pages deployments go green.
**Expected:** The published repositories carry the corrected provenance caveat and the corrected 04.1
coverage rows.
**Why human:** This verifier does not commit or push, and the published trees currently carry a
sentence the provider's own response contradicts.

**4. Mail routing for `haoo.online`** *(carry-forward to Phase 5 / LEAD-07)*
**Test:** Add MX at `mx1`/`mx2.privateemail.com` and send one test message to `info@haoo.online`.
**Expected:** It arrives.
**Why human:** DNS change plus a live send. Correctly out of this phase's scope by recorded decision,
but the deployed journey the goal invokes currently ends at an unreachable inbox.

### Gaps Summary

**No gaps.** No truth failed, no artifact is missing or a stub, no key link is unwired, no blocker
anti-pattern and no unreferenced debt marker exists in either repository. All eight requirement IDs
are accounted for with no orphans, and each Complete status reproduces on fresh measurement.

The phase goal is achieved in every respect that a repository, an artifact or a live host can
establish: HAOO **builds**, **tests** and **deploys** from `KaruguDev/HAOO` to `www.haoo.online` with
the apex 301-redirecting to it, ZERO-PAPER HUB does the same with no HAOO source, and the two trees
are provably disjoint outside a ratified 26-entry toolchain allowlist.

What is **not** established is the goal's third verb. HAOO is *configured to be* measured from its own
repository; whether it *is being* measured is unproven, because every count the report can produce is
zero and zero is not distinguishable from silence. That is one browser session away from being
settled, and it is the reason this phase reads `human_needed` rather than `passed`.

---

_Verified: 2026-09-06T19:40:13Z_
_Verifier: Claude (gsd-verifier)_
