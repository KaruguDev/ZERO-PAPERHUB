---
phase: 05-prove-the-deployed-journey
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/05-prove-the-deployed-journey/05-EVIDENCE-PLANNING-HOME.md
  - .planning/phases/05-prove-the-deployed-journey/05-PATTERNS.md
  - .planning/phases/05-prove-the-deployed-journey/05-VALIDATION.md
  - ../ZERO-PAPERHUB/.planning
autonomous: true
requirements: [QUAL-05]

estimate:
  tokens: 20000
  raw_tokens: 40000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "Every Phase 5 planning, research, pattern, UI-spec, validation and plan artifact is readable from the HAOO checkout at .planning/phases/05-prove-the-deployed-journey/"
    - "The ZERO-PAPER HUB checkout carries no .planning/ directory, and the removal commit records the re-run superset walk that authorised it"
    - "npm run verify:disjoint still exits 0 in both checkouts after the removal"
  artifacts:
    - path: ".planning/phases/05-prove-the-deployed-journey/05-EVIDENCE-PLANNING-HOME.md"
      provides: "The re-run D37 superset walk output, the removal commit SHAs in both repositories, and the working-directory rule for the rest of the phase"
      contains: "ZPH-only"
    - path: ".planning/phases/05-prove-the-deployed-journey/05-PATTERNS.md"
      provides: "The pattern map, mirrored out of the tree that is being removed"
    - path: ".planning/phases/05-prove-the-deployed-journey/05-VALIDATION.md"
      provides: "The Nyquist validation contract, mirrored out of the tree that is being removed"
  key_links:
    - from: ".planning/phases/05-prove-the-deployed-journey/05-EVIDENCE-PLANNING-HOME.md"
      to: "../ZERO-PAPERHUB/.planning"
      via: "records the measured walk that authorised the removal, re-run at execution time rather than cited"
      pattern: "differing"
  prohibitions:
    - statement: "The planning tree must not be removed on the strength of a recorded walk figure; the walk is re-run at execution time and its live output is what authorises the deletion."
      status: unverified
      verification: judgment
    - statement: "A file that differs between the two planning trees must not be silently resolved by picking a side; the divergence halts the task and is surfaced."
      status: unverified
      verification: judgment
---

<objective>
Make `KaruguDev/HAOO` the single home for the Phase 5 planning record (D-01) and execute the second
half of 04.2 D-03 by removing `KaruguDev/ZERO-PAPERHUB`'s `.planning/` directory (D-02) — after, and
only after, a walk re-run at execution time proves HAOO is a byte-for-byte superset.

**Working-directory rule for the whole of Phase 5 (D-01, D-04):** every plan in this phase executes
with `/home/paul/Documents/Vibe Coding Projects/HAOO` as the working directory. Every relative path
in every Phase 5 plan is HAOO-relative; the ZERO-PAPER HUB checkout is reached as
`../ZERO-PAPERHUB`, the same relative path `npm run verify:disjoint` already hardcodes. Exactly one
checkout exists per repository and no git worktree is created (D-04).

Purpose: nothing further in this phase is authored into a tree that is about to vanish, and the
removal is a deliberate planned act with its authorising measurement recorded, rather than a side
effect.
Output: `05-EVIDENCE-PLANNING-HOME.md` in HAOO, a removal commit in ZERO-PAPER HUB, and a green
`verify:disjoint` on both sides afterwards.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

## Artifacts this phase produces

Symbols, files and scripts Phase 5 creates (none of these exist in either tree before this phase, so
a drift check must not flag them as unacknowledged pre-existing symbols):

- `playwright.config.ts`, `tsconfig.e2e.json` (HAOO root, new)
- `e2e/` directory: `e2e/tracer.e2e.ts`, `e2e/viewport.e2e.ts`, `e2e/keyboard.e2e.ts`,
  `e2e/semantics.e2e.ts`, `e2e/zoom-motion.e2e.ts`, `e2e/recovery.e2e.ts`, `e2e/form-states.e2e.ts`,
  `e2e/axe-baseline.e2e.ts`
- `e2e/fixtures/surfaces.ts` exporting `SURFACES`, `SurfaceId`
- `e2e/fixtures/primary-actions.ts` exporting `PRIMARY_ACTIONS`, `PrimaryAction`, `MIN_PRIMARY_HIT_TARGET_PX`,
  `MIN_INTERACTIVE_HIT_TARGET_PX`
- `e2e/fixtures/viewports.ts` exporting `VIEWPORTS`, `ZOOM_VIEWPORTS`
- `e2e/fixtures/axe.ts` exporting `axeFor`, `AXE_TAGS`, `AXE_PER_URL_DISABLES`, `BLOCKING_IMPACTS`
- `e2e/fixtures/evidence.ts` exporting `recordEvidence`, `readEvidence`, `EvidenceRecord`
- `e2e/fixtures/overflow.ts` exporting `collectViewportEscapees`, `measureDocumentWidths`
- `.github/workflows/verify-split.yml` (HAOO, new)
- npm scripts `test:e2e`, `test:e2e:live` (HAOO `package.json`, new)
- `evidence/` directory at the HAOO repository root (machine-written run output, committed)
- `.planning/phases/05-prove-the-deployed-journey/05-EVIDENCE-*.md` family (new, both this plan and
  its successors)
- The D-16 successor script and npm script named by the checkpoint in plan 05-15
- `e2e/axe-baseline.e2e.ts`, `e2e/axe-gate.e2e.ts` and the closed accepted-finding exception list in the gate
- `e2e/live-submission.e2e.ts`, its two marker-purpose constants and its marker builder

<tasks>

<task type="auto">
  <name>Task 1: Sync every ZERO-PAPER HUB-only planning path into HAOO and prove the two trees agree</name>
  <precondition>Both checkouts exist at `/home/paul/Documents/Vibe Coding Projects/HAOO` and `/home/paul/Documents/Vibe Coding Projects/ZERO-PAPERHUB`, and `git status --short` is clean in both.</precondition>
  <read_first>
    - `.planning/phases/05-prove-the-deployed-journey/05-CONTEXT.md` — D-01, D-02, D-03, D-04
    - `.planning/ROADMAP.md` §"Phase 04.2" → **Post-verification follow-up** — the D-02 item and its W-1 amendment
    - `.planning/phases/04.2-split-haoo-into-its-own-repository-and-domain/04.2-DEFERRED-ITEMS.md` §D37 — the walk and its required step 0
    - `scripts/verify-tree-disjointness.mjs` lines 1-60 — `EXCLUDED_PREFIXES = ['.planning/']` and the header stating why the planning record is verified separately
  </read_first>
  <files>.planning/phases/05-prove-the-deployed-journey/05-PATTERNS.md, .planning/phases/05-prove-the-deployed-journey/05-VALIDATION.md, .planning/phases/05-prove-the-deployed-journey/05-EVIDENCE-PLANNING-HOME.md</files>
  <action>
    Walk both planning trees and reconcile them so HAOO is a strict superset, then record the walk.

    Enumerate every file under `../ZERO-PAPERHUB/.planning/` and under `.planning/`, excluding
    `.planning/research/.cache/` in both (it is gitignored in HAOO and is generated, not record).
    Classify each relative path into three sets: ZPH-only, HAOO-only, and shared.

    For every ZPH-only path, copy the file into the same relative path under HAOO's `.planning/`,
    creating parent directories as needed. The planner has already mirrored
    `phases/05-prove-the-deployed-journey/05-PATTERNS.md` and
    `phases/05-prove-the-deployed-journey/05-VALIDATION.md`, so the expected ZPH-only count at this
    point is zero — copy anything that is nevertheless present rather than assuming.

    For every shared path, compare the two files byte-for-byte. If ANY shared path differs, STOP:
    do not copy, do not delete, do not commit. Print each differing relative path together with the
    first twenty lines of its unified diff and halt the task. Choosing a winner is a decision the
    executor may not make on its own judgment — a divergence between the two planning records is the
    exact condition D-03's single-home rule exists to close, and it must be surfaced.

    HAOO-only paths are expected and correct — HAOO is the superset. `milestone.lock` is one of
    them. Record the count but do not act on them.

    Create `.planning/phases/05-prove-the-deployed-journey/05-EVIDENCE-PLANNING-HOME.md` and write
    the measured values into it in the discipline
    `.planning/phases/04.2-split-haoo-into-its-own-repository-and-domain/04.2-VERIFICATION.md`
    §"Human Verification Outcome" uses: the ISO-8601 UTC timestamp of the walk, the total file count
    compared, and the three set sizes as literal integers — the ZPH-only count, the HAOO-only count,
    and the differing count. Write the numbers, never the words "clean" or "passed". Add a heading
    stating that this figure is authorising the removal in the same commit and is not a citation of
    any earlier walk, per the 04.2 W-1 amendment.

    **Write the two authorising integers on their own labelled lines, in exactly this form**, each at
    the start of its own line with nothing before the label — task 2's gate greps these two lines and
    refuses to delete anything unless both read zero, so the format is a machine contract, not a
    presentation choice:

    - a line beginning `ZPH-only count:` followed by the integer
    - a line beginning `Differing count:` followed by the integer

    Any other prose in the file may mention these words freely; the gate is anchored to the
    line start, so it cannot be satisfied or defeated by a sentence elsewhere in the document.

    Commit the copied files and the evidence file in the HAOO checkout.
  </action>
  <acceptance_criteria>
    - `.planning/phases/05-prove-the-deployed-journey/05-PATTERNS.md` and `05-VALIDATION.md` both exist under HAOO's `.planning/` and are byte-identical to their `../ZERO-PAPERHUB/.planning/` counterparts (`cmp` exits 0 for each).
    - `05-EVIDENCE-PLANNING-HOME.md` contains an ISO-8601 UTC timestamp, a total compared-file count, and three integer counts labelled ZPH-only, HAOO-only and differing.
    - The file carries a line beginning `ZPH-only count:` whose integer is `0`, anchored at the start of the line.
    - The file carries a line beginning `Differing count:` whose integer is `0`, anchored at the start of the line.
    - `git status --short` is clean in the HAOO checkout after the commit.
  </acceptance_criteria>
  <verify>
    <automated>cmp .planning/phases/05-prove-the-deployed-journey/05-PATTERNS.md ../ZERO-PAPERHUB/.planning/phases/05-prove-the-deployed-journey/05-PATTERNS.md && cmp .planning/phases/05-prove-the-deployed-journey/05-VALIDATION.md ../ZERO-PAPERHUB/.planning/phases/05-prove-the-deployed-journey/05-VALIDATION.md && grep -qE '^ZPH-only count:[[:space:]]*0[[:space:]]*$' .planning/phases/05-prove-the-deployed-journey/05-EVIDENCE-PLANNING-HOME.md && grep -qE '^Differing count:[[:space:]]*0[[:space:]]*$' .planning/phases/05-prove-the-deployed-journey/05-EVIDENCE-PLANNING-HOME.md</automated>
  </verify>
  <done>HAOO's planning tree is a proven superset of ZERO-PAPER HUB's, and the walk that proves it is recorded with its measured integers in the same commit.</done>
</task>

<task type="auto">
  <name>Task 2: Remove the ZERO-PAPER HUB planning directory and record both removal commits</name>
  <reversibility rating="one-way">A deleted planning tree is recoverable only from git history, and the point of the removal is that HAOO becomes authoritative; restoring a second tree re-establishes the divergence problem 04.2 D-03 closed. The owner recorded this choice as D-02, so the door is already chosen — the rating is kept for the record and no checkpoint re-asks it.</reversibility>
  <precondition>Task 1's recorded ZPH-only count and differing count are both `0` in `05-EVIDENCE-PLANNING-HOME.md`.</precondition>
  <read_first>
    - `.planning/phases/05-prove-the-deployed-journey/05-EVIDENCE-PLANNING-HOME.md` — the walk this task acts on
    - `.planning/phases/05-prove-the-deployed-journey/05-CONTEXT.md` — D-02 and its reversibility note
    - `../ZERO-PAPERHUB/scripts/verify-tree-disjointness.mjs` lines 40-60 — `EXCLUDED_PREFIXES`, so the removal cannot be mistaken for a separation fix
  </read_first>
  <files>../ZERO-PAPERHUB/.planning, .planning/phases/05-prove-the-deployed-journey/05-EVIDENCE-PLANNING-HOME.md</files>
  <action>
    **Run the gate first, as a command, and let its exit status decide.** Before touching anything,
    run this exactly, from the HAOO checkout:

    `grep -qE '^ZPH-only count:[[:space:]]*0[[:space:]]*$' .planning/phases/05-prove-the-deployed-journey/05-EVIDENCE-PLANNING-HOME.md && grep -qE '^Differing count:[[:space:]]*0[[:space:]]*$' .planning/phases/05-prove-the-deployed-journey/05-EVIDENCE-PLANNING-HOME.md`

    If it exits non-zero, **halt the task**: do not delete, do not stage, do not commit. Report the
    two lines as they actually read. This is the machine-enforced precondition on a one-way act — the
    owner's decision to remove the tree is settled (D-02), but a settled decision authorises the act
    only while its precondition still holds, and the precondition is that no unique record is about to
    be destroyed. Prose telling an executor to check is not a guard; this command is.

    Chain the deletion to that gate in one shell invocation so the two cannot be separated by a
    re-read, a retry, or an executor's judgment — the `git rm` runs only as the right-hand side of the
    gate's success.

    Then, in the ZERO-PAPER HUB checkout, remove the whole `.planning/` directory with
    `git rm -r --quiet .planning` and commit it with the message
    `chore(05): remove the ZERO-PAPER HUB planning directory (04.2 D-03, phase 5 D-02)`. The commit
    body states, in prose: the ISO-8601 UTC timestamp of the authorising walk, the three integer
    counts, that `KaruguDev/HAOO` is now the single home for the planning record, and that
    `scripts/verify-tree-disjointness.mjs` excludes `.planning/` from both halves of the intersection
    so this removal is not a separation fix and cannot mask one. Delete any untracked residue left in
    `../ZERO-PAPERHUB/.planning/` after the `git rm` so the directory is genuinely gone rather than
    merely untracked.

    Append the ZERO-PAPER HUB removal commit SHA and the HAOO task-1 commit SHA to
    `05-EVIDENCE-PLANNING-HOME.md`, then commit that append in the HAOO checkout.
  </action>
  <acceptance_criteria>
    - The authorising gate was run as a command before the deletion, and its exit status was 0.
    - The two authorising lines still read `0` in `05-EVIDENCE-PLANNING-HOME.md` after the deletion — the file is committed in HAOO and is unaffected by removing the ZERO-PAPER HUB tree, so the authorisation remains checkable afterwards.
    - `../ZERO-PAPERHUB/.planning` does not exist as a directory on disk.
    - `git -C ../ZERO-PAPERHUB log -1 --format=%s` names the removal and cites `D-03`.
    - `git -C ../ZERO-PAPERHUB ls-files .planning` produces no output.
    - `05-EVIDENCE-PLANNING-HOME.md` contains both commit SHAs as 7-or-more-character hexadecimal strings.
    - `git -C ../ZERO-PAPERHUB status --short` is clean.
  </acceptance_criteria>
  <verify>
    <automated>grep -qE '^ZPH-only count:[[:space:]]*0[[:space:]]*$' .planning/phases/05-prove-the-deployed-journey/05-EVIDENCE-PLANNING-HOME.md && grep -qE '^Differing count:[[:space:]]*0[[:space:]]*$' .planning/phases/05-prove-the-deployed-journey/05-EVIDENCE-PLANNING-HOME.md && test ! -e ../ZERO-PAPERHUB/.planning && test -z "$(git -C ../ZERO-PAPERHUB ls-files .planning)" && test -z "$(git -C ../ZERO-PAPERHUB status --short)"</automated>
  </verify>
  <done>ZERO-PAPER HUB carries no planning directory, the removal is a recorded commit in each repository, and the evidence file names both SHAs.</done>
</task>

<task type="auto">
  <name>Task 3: Re-prove tree disjointness in both checkouts after the removal</name>
  <read_first>
    - `package.json` — `scripts.verify:disjoint`, which resolves the sibling as `../ZERO-PAPERHUB`
    - `scripts/verify-tree-disjointness.mjs` lines 459-530 — argument resolution against `SCRIPT_REPO_ROOT` and the counts printed in the success line
    - `.planning/phases/05-prove-the-deployed-journey/05-EVIDENCE-PLANNING-HOME.md` — the file this task appends to
  </read_first>
  <files>.planning/phases/05-prove-the-deployed-journey/05-EVIDENCE-PLANNING-HOME.md</files>
  <action>
    Run `npm run verify:disjoint` in the HAOO checkout and then in the ZERO-PAPER HUB checkout.
    Both must exit 0. Capture each run's full success line verbatim — the line reporting paths
    compared, shared paths, allowlist entries, allowlist subtracted, violations, ratified collisions
    and the two carrier counts.

    Append both verbatim lines to `05-EVIDENCE-PLANNING-HOME.md` under a heading naming the two
    repositories and the run timestamp, with each repository's exit code recorded as an integer
    beside its line. Record the numbers the run printed, not a pass mark — the counts are what a
    later reader compares against, and 04.2's discipline is that a count with no provenance is not
    evidence.

    Also append the working-directory rule for the rest of the phase in one short paragraph: every
    remaining Phase 5 plan runs with the HAOO checkout as the working directory and reaches
    ZERO-PAPER HUB as `../ZERO-PAPERHUB`; exactly one checkout exists per repository and no git
    worktree is created (D-04). Name the Phase 4 incident that rule exists to prevent — a leftover
    agent-tool worktree made Vitest collect every suite twice — so a later reader sees the cost
    rather than only the rule.

    Commit the appended evidence in the HAOO checkout.
  </action>
  <acceptance_criteria>
    - `npm run verify:disjoint` exits 0 in the HAOO checkout.
    - `npm run verify:disjoint` exits 0 in the `../ZERO-PAPERHUB` checkout.
    - `05-EVIDENCE-PLANNING-HOME.md` contains the substring `violations:` at least twice, once per repository.
    - `05-EVIDENCE-PLANNING-HOME.md` states that the remaining Phase 5 plans run from the HAOO checkout and names `../ZERO-PAPERHUB` as the sibling path.
  </acceptance_criteria>
  <verify>
    <automated>npm run verify:disjoint && (cd ../ZERO-PAPERHUB && npm run verify:disjoint) && test "$(grep -c 'violations:' .planning/phases/05-prove-the-deployed-journey/05-EVIDENCE-PLANNING-HOME.md)" -ge 2</automated>
  </verify>
  <done>SPLT-01 is re-proven on both sides after the removal, with both success lines recorded verbatim.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| working tree → git history | A destructive removal crosses from recoverable working state into a commit; the only recovery is history |
| HAOO planning record → public repository | Both repositories are PUBLIC; anything written into `.planning/` is world-readable |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Denial of service | `../ZERO-PAPERHUB/.planning` removal | high | mitigate | Task 2 halts unless task 1's recorded ZPH-only and differing counts are both `0`; the sync runs before the walk and the walk before the delete, so no unique record can be destroyed |
| T-05-02 | Repudiation | the authorising walk | medium | mitigate | The walk is re-run at execution time and its integers are written into the same commit as the deletion; a recorded figure from an earlier session is never accepted as the precondition |
| T-05-03 | Tampering | `scripts/verify-tree-disjointness.mjs` semantics | medium | mitigate | Task 3 re-runs `verify:disjoint` in both checkouts and records both success lines, so the removal cannot silently change what SPLT-01 measures |
| T-05-04 | Information disclosure | evidence file in a public repository | low | accept | The evidence file carries file counts, commit SHAs and tool output only — no credentials, no mailbox content, no personal data |
</threat_model>

<verification>
- HAOO's planning tree contains every path the ZERO-PAPER HUB tree contained, byte-identical.
- `../ZERO-PAPERHUB/.planning` is absent from disk and from the git index.
- `npm run verify:disjoint` exits 0 in both checkouts.
- `05-EVIDENCE-PLANNING-HOME.md` records measured integers and verbatim tool output, not pass marks.
</verification>

<success_criteria>
- The Phase 5 planning record has exactly one home, and the removal that made it so is authorised by a walk re-run in the same commit.
- Both repositories still satisfy SPLT-01 after the removal, proven by a fresh run in each.
- Every subsequent Phase 5 plan has a stated, unambiguous working directory.
</success_criteria>

<output>
Create `.planning/phases/05-prove-the-deployed-journey/05-01-SUMMARY.md` when done
</output>
