---
phase: 05-prove-the-deployed-journey
plan: 04
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/components/ProductHeader.tsx
  - src/pages/ProductPage.tsx
  - src/test/haoo-page.test.tsx
  - src/test/focus-contrast.test.ts
  - .planning/phases/05-prove-the-deployed-journey/05-EVIDENCE-PREFLIGHT-FIXES.md
autonomous: true
requirements: [QUAL-02, QUAL-03]

estimate:
  tokens: 16000
  raw_tokens: 32000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "Both `Back to ZERO-PAPER HUB` links on the HAOO page resolve to the parent site at https://www.zero-paperhub.com/ rather than looping the visitor back to the page they are already on"
    - "The jsdom assertion that pinned the wrong destination is corrected in the same commit as the components, so a correct fix is not reverted by a red suite"
    - "There are still exactly two `Back to ZERO-PAPER HUB` links on the page — the count assertion is untouched"
    - "`src/components/MeasurementDisclosure.tsx` is registered in the closed FOCUS_SOURCES list, so its declared focus rings are measured by the existing contrast gate"
    - "The focus-contrast gate's machinery is byte-unchanged: the minimum ratio, the ring-colour token map, the default offset, the extractor and the non-empty-pairs guard all keep their pre-plan values"
  artifacts:
    - path: "src/components/ProductHeader.tsx"
      provides: "The header parent-site link, pointing at the parent site"
      contains: "https://www.zero-paperhub.com/"
    - path: "src/pages/ProductPage.tsx"
      provides: "The footer parent-site link, pointing at the parent site"
      contains: "https://www.zero-paperhub.com/"
    - path: "src/test/focus-contrast.test.ts"
      provides: "The closed focus-source list, widened from six entries to seven with the reason recorded in the list's own doc-comment"
      contains: "src/components/MeasurementDisclosure.tsx"
    - path: ".planning/phases/05-prove-the-deployed-journey/05-EVIDENCE-PREFLIGHT-FIXES.md"
      provides: "The record of the two pre-flight findings closed here and the three deferred to a future ZERO-PAPER HUB phase, with their measured values"
      contains: "F4"
  key_links:
    - from: "src/test/haoo-page.test.tsx"
      to: "src/components/ProductHeader.tsx"
      via: "the href assertion that guarded the defect, corrected in the same commit as the component"
      pattern: "www\\.zero-paperhub\\.com"
    - from: "src/test/focus-contrast.test.ts"
      to: "src/components/MeasurementDisclosure.tsx"
      via: "closed-list registration; the extractor reads the component's Tailwind ring literals"
      pattern: "MeasurementDisclosure"
  prohibitions:
    - statement: "A component fix must not be reverted because the assertion guarding the defect went red; the guarding assertion is part of the fix, not collateral damage."
      status: resolved
      verification: judgment
    - statement: "The measured, unfixed ZERO-PAPER HUB defects F4, F5 and F6 must not be recorded as absent, not-applicable or fixed; they are recorded as measured, unfixed and deferred by a scope decision, never by a severity judgement."
      status: resolved
      verification: judgment
    - statement: "Registering a component in the closed focus list must not be achieved by widening the ring-colour token map or relaxing the contrast floor; a token the map does not know must fail loudly."
      status: resolved
      verification: judgment
---

<objective>
Close the two pre-flight findings the UI-SPEC placed in scope — F1, where both `Back to
ZERO-PAPER HUB` links point at the HAOO page itself and a green jsdom assertion guards the defect,
and F3, where a focus-bearing component sits outside the closed list that exists to measure focus
styles — and record the three ZERO-PAPER HUB findings the owner deferred, so they are inherited
rather than rediscovered.

Both fixes land before the semantics and keyboard specs in wave 4 assert against them, so those
specs measure a correct page rather than pinning a defect a second time.

Purpose: the phase boundary permits fixing a defect the evidence reveals. These two were revealed
while the UI design contract was being written, and both are one-line changes with a guarding test
that must move with them.
Output: two corrected links, one widened closed list, and `05-EVIDENCE-PREFLIGHT-FIXES.md`.

**Working directory: the HAOO checkout** (05-01, D-01/D-04). Nothing here touches `../ZERO-PAPERHUB`;
D-OQ-3 stops ZERO-PAPER HUB-side evidence at the Products section and defers its three findings.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

## Artifacts this phase produces

See `05-01-PLAN.md` § "Artifacts this phase produces" for the full list. This plan creates one new
file, `.planning/phases/05-prove-the-deployed-journey/05-EVIDENCE-PREFLIGHT-FIXES.md`. Every other
file it touches — `src/components/ProductHeader.tsx`, `src/pages/ProductPage.tsx`,
`src/test/haoo-page.test.tsx`, `src/test/focus-contrast.test.ts` — already exists and ships today; no
new symbol is introduced in any of them, and the `FOCUS_SOURCES` array gains one entry rather than a
new export.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Point both parent-site links at the parent site, and move the assertion that guarded the defect</name>
  <read_first>
    - `src/components/ProductHeader.tsx` lines 28-40 — the header anchor and its `focusClasses` composition
    - `src/pages/ProductPage.tsx` lines 300-312 — the footer link row and its `footerLinkClasses` composition
    - `src/test/haoo-page.test.tsx` lines 44-58 — the assertion that pins the destination, currently green *because* the link is wrong
    - `src/test/haoo-page.test.tsx` lines 396-406 — the count-of-two assertion, which is correct and must not move
    - `src/test/measurement-page.test.tsx` lines 852-872 — the footer-ordering assertion that also names this link but does not assert its destination
    - `.planning/phases/05-prove-the-deployed-journey/05-UI-SPEC.md` §"F1" — the two-file-plus-test scope and why doing only one half gets reverted
    - `.planning/phases/04.2-split-haoo-into-its-own-repository-and-domain/04.2-SPLIT-CONTRACT.md` §Domain → Decision (a) reversal — why `www.zero-paperhub.com` is the parent-site URL and `www.haoo.online` is canonical for HAOO
  </read_first>
  <files>src/test/haoo-page.test.tsx, src/components/ProductHeader.tsx, src/pages/ProductPage.tsx</files>
  <behavior>
    - Both links whose accessible name is `Back to ZERO-PAPER HUB` have a destination equal to `https://www.zero-paperhub.com/` — an absolute URL on the parent site's canonical `www` leg.
    - There are still exactly two such links in the rendered page: one in the header, one in the footer.
    - The footer link still renders after the `How we measure this page` link in DOM order, so the existing ordering assertion in `src/test/measurement-page.test.tsx` stays green untouched.
  </behavior>
  <action>
    Change the assertion first, watch it go red, then change the components.

    In `src/test/haoo-page.test.tsx` around line 52, the assertion currently requires every link named
    `Back to ZERO-PAPER HUB` to have a same-origin root destination. Change the expected value to the
    absolute parent-site URL `https://www.zero-paperhub.com/`. Leave the count assertion near line 403
    at two — that number is correct and asserting it is what stops a future edit from quietly dropping
    one of the links. Run the suite and confirm it is now red on exactly this assertion; that red is
    the proof the assertion was guarding the defect.

    Then change `src/components/ProductHeader.tsx` line 33 and `src/pages/ProductPage.tsx` line 308:
    both anchors' destination becomes `https://www.zero-paperhub.com/`. Change nothing else on either
    anchor — the class composition, the accessible text, the `min-h-11` hit-target floor and the
    focus-ring utilities all stay exactly as they are. Do not add a `target` or `rel` attribute: this
    is a same-site-family navigation to the parent, not a new-tab link, and adding one would change a
    shipped interaction this phase's boundary does not permit changing.

    Run the suite again and confirm it is green. Both halves land in one commit — an executor who
    commits only the components leaves a red suite and the next reader reverts a correct fix.

    Record in the commit body what was wrong and why nothing that checks status codes could have
    caught it: a same-origin root destination on the HAOO host returns 200 and renders a valid page,
    so only an assertion comparing a link's *promise* against its *destination* surfaces it.
  </action>
  <acceptance_criteria>
    - `src/components/ProductHeader.tsx` contains the exact string `https://www.zero-paperhub.com/` on the anchor whose text is `Back to ZERO-PAPER HUB`.
    - `src/pages/ProductPage.tsx` contains the exact string `https://www.zero-paperhub.com/` on the footer anchor whose text is `Back to ZERO-PAPER HUB`.
    - `src/test/haoo-page.test.tsx` expects `https://www.zero-paperhub.com/` for those links.
    - `src/test/haoo-page.test.tsx` still asserts the count of those links is `2`.
    - `npx vitest run src/test/haoo-page.test.tsx src/test/measurement-page.test.tsx` exits 0.
    - `npm test` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/test/haoo-page.test.tsx src/test/measurement-page.test.tsx && grep -c 'https://www.zero-paperhub.com/' src/components/ProductHeader.tsx src/pages/ProductPage.tsx src/test/haoo-page.test.tsx && grep -q 'toHaveLength(2)' src/test/haoo-page.test.tsx</automated>
  </verify>
  <done>Both parent-site links resolve to the parent site, the count is still two, and the assertion that had been pinning the wrong value moved in the same commit.</done>
</task>

<task type="auto">
  <name>Task 2: Register MeasurementDisclosure in the closed focus-source list</name>
  <read_first>
    - `src/test/focus-contrast.test.ts` lines 1-60 — the named constants and their standard-citing comments, the list's doc-comment and its NARROWED history entry, the six current entries, and the non-empty-pairs vacuity guard
    - `src/components/MeasurementDisclosure.tsx` line 13 — the ring utilities the extractor will read, declared on both a summary element and a button
    - `.planning/phases/05-prove-the-deployed-journey/05-UI-SPEC.md` §"F3" and §"Resolved Decisions" D-OQ-4 — the owner's decision to register it, its recorded reason, and the explicit scope limit of one line
    - `shared-scaffold.txt` GROUND B — why this file is a ratified path collision that must stay divergent between the two repositories
  </read_first>
  <files>src/test/focus-contrast.test.ts</files>
  <action>
    Add `'src/components/MeasurementDisclosure.tsx'` as a seventh entry to the `FOCUS_SOURCES` array,
    and extend that array's doc-comment with a WIDENED history entry in the same idiom as the existing
    NARROWED one: state that the list went from six entries to seven, name the plan that did it, name
    the owner decision D-OQ-4 that authorised it, and state the reason — the component declares focus
    rings on a summary element and a button but had never been registered, and the list's own comment
    already says a new interactive component is admitted by being registered here. State explicitly
    which parts are byte-unchanged: the minimum-contrast constant, the ring-colour token map, the
    default ring offset, the extractor, and the non-empty-pairs guard — so the seven entries are
    measured exactly as strictly as the six were.

    Do not touch `src/components/MeasurementDisclosure.tsx` itself. Its copy is governed by locked
    owner approval from 04.2, and this registration changes the test's source list only.

    Do not add any token to the ring-colour map. The component's ring colour is an arbitrary hex the
    extractor already handles; if the run instead throws on an unrecognised token, that is the list
    failing loudly as designed — report it rather than widening the map to make it pass.

    Run `npx vitest run src/test/focus-contrast.test.ts` and confirm it exits 0 with seven sources
    measured. Do not cite any pre-computed contrast figure as the reason it passes — confirm it by
    running the suite.

    Run `npm run verify:disjoint` afterwards: this file is a ratified GROUND B path collision whose
    two copies must remain divergent, and a widening on the HAOO side keeps them so. The auditor
    reports convergence as a violation, so a green run here is the proof the ratification still holds.
  </action>
  <acceptance_criteria>
    - `src/test/focus-contrast.test.ts` `FOCUS_SOURCES` has exactly seven entries, the new one being `src/components/MeasurementDisclosure.tsx`.
    - The `FOCUS_SOURCES` doc-comment names the plan that widened it, cites D-OQ-4, and lists the machinery that stayed byte-unchanged.
    - `git diff` for this task touches `src/test/focus-contrast.test.ts` only.
    - `npx vitest run src/test/focus-contrast.test.ts` exits 0.
    - `npm run verify:disjoint` exits 0 and its output reports `converged: 0`.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/test/focus-contrast.test.ts && npm run verify:disjoint && node -e "const s=require('fs').readFileSync('src/test/focus-contrast.test.ts','utf8'); const m=s.match(/FOCUS_SOURCES = \[([\s\S]*?)\] as const/); if(!m) throw new Error('list not found'); const n=(m[1].match(/'src\//g)||[]).length; if(n!==7) throw new Error('entries='+n); if(!m[1].includes('MeasurementDisclosure')) throw new Error('not registered');"</automated>
  </verify>
  <done>The seventh focus source is registered, the gate's machinery is untouched, and the two repositories' copies of the file are still divergent.</done>
</task>

<task type="auto">
  <name>Task 3: Record the two findings closed here and the three deferred, with their measured values</name>
  <read_first>
    - `.planning/phases/05-prove-the-deployed-journey/05-UI-SPEC.md` §"Pre-Flight Findings" F1 through F6 and §"Deferred to a Future ZERO-PAPER HUB Phase" — the measured figures and file locations to transcribe
    - `.planning/phases/05-prove-the-deployed-journey/05-UI-SPEC.md` §"Resolved Decisions" D-OQ-3 — the recorded reason the three are a scope decision rather than a severity judgement
    - `.planning/phases/04.2-split-haoo-into-its-own-repository-and-domain/04.2-VERIFICATION.md` §"Human Verification Outcome" — the record-measured-values discipline
  </read_first>
  <files>.planning/phases/05-prove-the-deployed-journey/05-EVIDENCE-PREFLIGHT-FIXES.md</files>
  <action>
    Create `05-EVIDENCE-PREFLIGHT-FIXES.md` with two sections.

    **Closed in this plan.** One row each for F1 and F2 and F3. For F1: the two source locations, the
    destination before and after, the test assertion that moved, and the count assertion that did not.
    For F2: state that no source change was made — the horizontal-clipping utility on both root
    wrappers stays exactly as shipped, and the finding is closed by *method* rather than by edit,
    through the three-part overflow measurement the tracer already implements and plan 05-08 expands.
    For F3: the list size before and after, the component registered, the owner decision that
    authorised it, and the machinery that stayed byte-unchanged.

    **Deferred to a future ZERO-PAPER HUB phase.** One row each for F4, F5 and F6, transcribed with
    their measured values rather than summarised:
    - F4: the ring colour and its hex, the surface it is painted on, the measured contrast ratio, the
      floor it falls below, the file and approximate line range, and the fact that the native
      indicator is removed. State plainly that this is a live, public WCAG 2.2 SC 1.4.11 failure and
      that it is unfixed. Include F4b, the guard gap that let it go uncaught: that repository's closed
      focus list holds one entry and does not include the file carrying the failing ring.
    - F5: the absent main landmark and absent skip link on the ZERO-PAPER HUB home page, with the
      measured grep count, and the note that the Products-section axe run is *scoped* rather than
      rule-disabled precisely so this finding is not suppressed.
    - F6: the animation utilities and observer-driven reveals present with no reduced-motion handling
      anywhere in that repository, and the secondary risk that content starting fully transparent and
      depending on an observer is content a visitor loses entirely if the observer never fires.

    For all three, record the disposition in the owner's terms: **deferred by decision D-OQ-3, a scope
    decision and not a severity judgement**, with the recorded reason — asserting them would expand a
    proving phase into a remediation phase in a repository that receives code commits only when the
    evidence forces them. State that nobody has decided these are acceptable to leave indefinitely.

    Commit the file.
  </action>
  <acceptance_criteria>
    - `05-EVIDENCE-PREFLIGHT-FIXES.md` has a section for findings closed here and a section for findings deferred.
    - The deferred section names F4, F5 and F6 and states for each that it is measured, unfixed, and deferred.
    - The F4 row records a measured contrast ratio and the 3:1 floor it falls below, and names the failure as live and public.
    - The file records that the deferral is a scope decision under D-OQ-3 and not a severity judgement.
    - The file records F2 as closed by measurement method with no source change.
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'F4' .planning/phases/05-prove-the-deployed-journey/05-EVIDENCE-PREFLIGHT-FIXES.md && grep -q 'F5' .planning/phases/05-prove-the-deployed-journey/05-EVIDENCE-PREFLIGHT-FIXES.md && grep -q 'F6' .planning/phases/05-prove-the-deployed-journey/05-EVIDENCE-PREFLIGHT-FIXES.md && grep -q 'D-OQ-3' .planning/phases/05-prove-the-deployed-journey/05-EVIDENCE-PREFLIGHT-FIXES.md && grep -qi 'unfixed' .planning/phases/05-prove-the-deployed-journey/05-EVIDENCE-PREFLIGHT-FIXES.md</automated>
  </verify>
  <done>The two closed findings and the three deferred ones are recorded with their measured values, so a future ZERO-PAPER HUB phase inherits them instead of rediscovering them.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HAOO page → parent site | An outbound navigation to a different origin under the same owner |
| closed focus list → shipped focus styles | The list decides which declared focus indicators are measured at all |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-14 | Spoofing | the parent-site link destination | medium | mitigate | The destination is pinned to the exact absolute URL in both components and in the jsdom assertion, so a future edit to a different host reddens the suite rather than silently redirecting visitors |
| T-05-15 | Tampering | reverse tabnabbing | low | accept | No `target="_blank"` is introduced on these links, so no new-tab opener relationship is created; the one new-tab link on the page already carries its opener protection and is asserted by the brochure contract |
| T-05-16 | Repudiation | the closed focus list | medium | mitigate | The widening is recorded in the list's own doc-comment with the authorising decision and the untouched machinery named, so a later reader can tell a registration from a relaxation |
| T-05-17 | Information disclosure | `05-EVIDENCE-PREFLIGHT-FIXES.md` in a public repository | low | accept | The file records source locations, contrast ratios and rule references — all of which are already inspectable in the public source and on the public page |
</threat_model>

<verification>
- `npm test` exits 0 with both link destinations corrected and the guarding assertion moved.
- `npx vitest run src/test/focus-contrast.test.ts` exits 0 with seven registered sources.
- `npm run verify:disjoint` exits 0 with zero converged collisions.
- The three deferred ZERO-PAPER HUB findings are recorded with measured values and an explicit unfixed status.
</verification>

<success_criteria>
- A link whose accessible name names a destination now resolves to that destination.
- Every focus-bearing HAOO component is inside the list that measures focus styles.
- The deferred defects are inherited by name, value and reason rather than by memory.
</success_criteria>

<output>
Create `.planning/phases/05-prove-the-deployed-journey/05-04-SUMMARY.md` when done
</output>
