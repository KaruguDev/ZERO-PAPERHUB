---
phase: 05-prove-the-deployed-journey
plan: 10
type: execute
wave: 4
depends_on: ["05-05", "05-04"]
files_modified:
  - e2e/semantics.e2e.ts
  - .planning/phases/05-prove-the-deployed-journey/05-EVIDENCE-SEMANTICS.md
autonomous: true
requirements: [QUAL-03]

estimate:
  tokens: 29000
  raw_tokens: 58000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "The live HAOO page has exactly one top-level heading, and the ordered list of heading levels in document order never steps forward by more than one"
    - "Heading order is asserted in each state that renders a conditional heading, not only in the default state"
    - "The HAOO page exposes exactly one banner, one main and one contentinfo landmark, two named navigation landmarks with distinct names, and a labelled-region set equal to the closed expected list"
    - "Every link, button and form control in the traversal has a non-empty accessible name, and no accessible name is composed solely of decorative icon content"
    - "Every link whose accessible name names a destination resolves to that destination"
    - "The brochure's substantive content exists as real HTML on the page: all ten capability titles with their descriptions, and every journey step in document order"
    - "The HTML equivalent still holds when the source document artifact is unavailable, and the recovery copy never replaces the brochure controls"
    - "The head-level machine-discoverable pointer, the open action and the download action resolve to one and the same target, asserted against one another rather than each against a literal"
  artifacts:
    - path: "e2e/semantics.e2e.ts"
      provides: "The SS-1 through SS-4 measurements: heading order per state, landmarks, accessible names, and the brochure HTML equivalence under artifact failure"
      contains: "allInnerTexts"
    - path: ".planning/phases/05-prove-the-deployed-journey/05-EVIDENCE-SEMANTICS.md"
      provides: "The recorded heading-level sequence per state, the landmark and region inventories, the name-to-destination table, and the equivalence counts"
      contains: "10"
  key_links:
    - from: "e2e/semantics.e2e.ts"
      to: "src/components/ProductHeader.tsx"
      via: "the names-promise-destinations assertion, which is the rule that catches a link naming one destination and resolving to another"
      pattern: "zero-paperhub"
    - from: "e2e/semantics.e2e.ts"
      to: "src/test/build-output.test.ts"
      via: "the brochure path and product URL constants are reused rather than retyped, so the built-tree assertion and the live assertion cannot diverge"
      pattern: "brochure"
  prohibitions:
    - statement: "Heading order must not be asserted once in the default state and reported as covering the page; the conditional error, confirmation and fallback headings hold their levels only in the states that render them."
      status: unverified
      verification: judgment
    - statement: "The three brochure references must not be compared as raw attribute strings; one is relative on the live page, so resolved destinations are compared to one another."
      status: unverified
      verification: judgment
    - statement: "The HTML equivalent must not be allowed to depend on the artifact it is the equivalent of; it is asserted with that artifact unavailable."
      status: unverified
      verification: judgment
---

<objective>
Prove QUAL-03's structural half on the live HAOO page: semantic heading order, landmarks, descriptive
names, and an HTML equivalent to the brochure that survives the brochure being unavailable.

Two things here are load-bearing beyond the obvious. First, heading order is the item QUAL-03 names
most explicitly and is exactly the item the conformance rule tags do not check — the rule that checks
it is advisory-tagged, so it is either added explicitly or asserted by this spec, and the UI design
contract already enumerates the expected structure so this spec carries it as the load-bearing check
with the engine as corroboration. Second, "names promise destinations truthfully" is the rule that
caught a link naming the parent site and resolving to the page it was already on — plan 05-04 fixed
that, and this assertion is what stops it recurring.

Purpose: a page that reads correctly to a screen reader, and a brochure whose content is not locked
inside a document.
Output: `e2e/semantics.e2e.ts` and `05-EVIDENCE-SEMANTICS.md`.

**Working directory: the HAOO checkout** (05-01, D-01/D-04).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

## Artifacts this phase produces

See `05-01-PLAN.md` § "Artifacts this phase produces". This plan adds: `e2e/semantics.e2e.ts` and
`.planning/phases/05-prove-the-deployed-journey/05-EVIDENCE-SEMANTICS.md`.

## Planner assumptions carried forward (surfaced, not dropped)

- **QUAL-03 / empty probe edge — "What is the result for empty, single-element, or null input?"
  (unresolved).** Reviewed here and partially resolved by assertion: the empty case that matters for
  QUAL-03 is the brochure artifact being unavailable, and that case *is* asserted (task 2, the aborted
  artifact route). The cases that remain assumptions: a product whose capability list is empty and a
  product whose journey list is empty are unreachable at run time because every remaining product
  member is required and a partially-populated product fails the type gate rather than reaching a
  runtime state. The planner carries that as an assumption rather than an assertion — no live surface
  can exhibit it.

- **QUAL-03 / encoding probe edge — "Whose definition of length/equality applies — bytes, code points,
  grapheme clusters, or normalized form?" (unresolved).** Reviewed here and answered as a stated
  convention rather than left open: this spec compares **resolved URL strings** for destinations
  (never raw attribute strings, because one of the three brochure references is relative on the live
  page), and compares **accessible-name strings as the accessibility tree exposes them**, with no
  normalisation applied on either side. Counts — the ten capability titles, the journey steps, the
  region list — are compared as **element counts**, not string lengths. The one place a percent-encoded
  form is compared is the messaging action's starter text, which plan 05-11 decodes before comparing
  to the compile-time constant. No grapheme-cluster or Unicode-normalisation equality is performed
  anywhere, and the planner carries the assumption that the shipped copy contains no character
  sequence where normalised and unnormalised forms would differ.

<tasks>

<task type="auto">
  <name>Task 1: Assert heading order in every state, and the landmark and region inventories</name>
  <read_first>
    - `.planning/phases/05-prove-the-deployed-journey/05-UI-SPEC.md` §"SS-1" — the single top-level heading rule, the no-skipped-levels rule, the explicit expected level structure, and the requirement to assert conditional headings per state
    - `.planning/phases/05-prove-the-deployed-journey/05-UI-SPEC.md` §"SS-2" — the landmark table, the closed labelled-region list, and the recorded-as-observation treatment of the Products page's missing main landmark
    - `src/test/haoo-page.test.tsx` lines 44-58 — the closed region list this spec's expected list must equal, so the jsdom and live assertions pin the same set
    - `src/pages/ProductPage.tsx` — the section structure and the labelled regions in document order
    - `.planning/phases/05-prove-the-deployed-journey/05-RESEARCH.md` §"Pitfall 1" — why the conformance tag set does not check heading order and why this spec's assertion is the load-bearing one
    - `e2e/fixtures/surfaces.ts`, `e2e/fixtures/evidence.ts`
  </read_first>
  <files>e2e/semantics.e2e.ts</files>
  <action>
    **Headings.** Capture the ordered list of heading levels and texts in document order on the live
    HAOO page. Assert exactly one top-level heading exists and that its text equals the product's
    outcome string. Assert that for the ordered level list, every forward step is at most one — a
    level sequence that jumps is a skipped level regardless of how the markup is nested.

    Assert the expected structure explicitly rather than only structurally: the top-level heading is
    the hero, second-level headings cover the audiences section, benefits, capabilities, the journey,
    the brochure, the qualification section and the two onboarding placements; third-level headings
    cover the pain and benefit items, the capability cards, the journey steps, and the conditional
    error-summary, confirmation, failure and brochure-fallback headings.

    Assert the conditional headings **in the states that render them**, not once in the default state.
    Live, that means the error-summary heading after an empty-required submit, and the brochure
    fallback heading with the artifact route aborted. The confirmation and failure headings are
    reached only on the preview target and are asserted by plan 05-12; name them here as covered
    elsewhere so this file's coverage is legible rather than silently partial.

    Put a comment at the heading block recording why this spec owns the check: the rule that checks
    heading order is advisory-tagged in the engine and is therefore outside the conformance tag set,
    so the engine result is corroboration and this assertion is the load-bearing one. Warn, in the
    same comment, about the signal that would mean the corroboration has silently stopped running:
    an engine result reporting no heading-order outcome at all across every state.

    **Landmarks.** On the HAOO page assert exactly one banner, exactly one main and exactly one
    contentinfo; and exactly two navigation landmarks whose accessible names are distinct and match
    the shipped desktop and mobile navigation labels. Capture the accessible names of all labelled
    regions and assert the resulting list equals the closed expected list — the same set the jsdom
    suite already pins, plus the three onboarding-choice regions and the qualification region.

    On the Products surface assert the Products section is a region named for products via its
    labelling relationship, and that the Products navigation entry's destination resolves to that
    region's identifier. Record — do not assert — that no main landmark exists on that page, writing
    its measured value into the evidence; it is deferred by decision D-OQ-3.

    Record the ordered heading list per state and both landmark inventories through the shared
    recorder.
  </action>
  <acceptance_criteria>
    - The spec asserts the top-level heading count is exactly 1 on the HAOO page.
    - The spec asserts the ordered heading-level list never steps forward by more than one, and reports the offending pair on failure.
    - The spec captures and asserts heading order in at least three distinct states on the live page, including the error-summary state and the aborted-artifact state.
    - The spec asserts exactly one banner, one main and one contentinfo, and exactly two distinctly-named navigation landmarks.
    - The spec asserts the labelled-region name list equals a closed expected list declared in the spec, and that list matches the one pinned in `src/test/haoo-page.test.tsx`.
    - The Products page's missing main landmark is recorded through the evidence recorder and is not asserted.
    - `npx playwright test --project=live e2e/semantics.e2e.ts` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test --project=live e2e/semantics.e2e.ts && node -e "const s=require('fs').readFileSync('e2e/semantics.e2e.ts','utf8'); for(const r of ['Benefits','Capabilities','Rental journey','Brochure','Onboarding']) if(!s.includes(r)) throw new Error('expected region missing from closed list: '+r); if(!/banner/.test(s)||!/contentinfo/.test(s)) throw new Error('landmark assertions incomplete');"</automated>
  </verify>
  <done>Heading order is asserted per state by this spec rather than delegated to an advisory rule, and both landmark inventories are pinned to closed lists.</done>
</task>

<task type="auto">
  <name>Task 2: Assert descriptive names, truthful destinations, and the brochure HTML equivalent under artifact failure</name>
  <read_first>
    - `.planning/phases/05-prove-the-deployed-journey/05-UI-SPEC.md` §"SS-3" — the name rules, the decorative-icon negative, the embed's label, the label-and-description resolution rules, and the names-promise-destinations rule
    - `.planning/phases/05-prove-the-deployed-journey/05-UI-SPEC.md` §"SS-4" — the five equivalence assertions, including the aborted-artifact re-assertion and the three-reference equality
    - `.planning/phases/05-prove-the-deployed-journey/05-RESEARCH.md` §"Code Examples" 4 and 5 — the artifact-abort pattern and the resolved-URL comparison
    - `src/test/build-output.test.ts` lines 29-55 — the named constants for the product URL and the brochure alternate link, reused here rather than retyped
    - `src/components/BrochurePanel.tsx` — the two layout branches, the embed's fallback subtree and the two sibling actions
    - `src/products/haoo.ts` — the capability list and the journey step list, the source of the counts this spec asserts
    - `src/components/ProductHeader.tsx`, `src/pages/ProductPage.tsx` — the two parent-site links plan 05-04 corrected, whose destinations this assertion now guards
  </read_first>
  <files>e2e/semantics.e2e.ts</files>
  <action>
    **Names.** Assert every link, button and form control in the traversal has a non-empty accessible
    name. Assert every image has a non-empty alternative text except those marked decorative, and
    assert the decorative-icon rule as a negative: no accessible name in the traversal is composed
    solely of icon content. Assert the brochure embed's label equals the product name followed by the
    brochure-preview suffix. Assert every form control's name comes from a label whose association
    resolves, and that every description reference resolves to an element that exists.

    **Names promise destinations truthfully.** For every link whose accessible name names a
    destination — a phone number, an email address, a host, or another site — assert the resolved
    destination is that destination. Comment at this assertion that it is the rule that caught the two
    parent-site links, that nothing checking status codes could have caught them because a same-origin
    root destination returns a valid page, and that only comparing a link's promise against its
    destination surfaces that class of defect. Where the same accessible name renders more than once,
    assert the destinations are identical rather than asserting the name is unique.

    **The brochure HTML equivalent.** Assert all five equivalence conditions:
    1. Every capability title in the product data appears as a third-level heading inside the
       capabilities section, and its description as text in the same list item. Assert **count
       equality** — ten of ten — not merely presence, so a silently dropped capability fails.
    2. Every journey step's title and description appears, in document order, inside the journey
       section's ordered list.
    3. Abort the artifact route and re-assert 1 and 2 unchanged. The equivalent must not depend on the
       artifact it is the equivalent of.
    4. In that same aborted state, assert the embed's fallback subtree renders the preview-unavailable
       heading and its body copy, and that both brochure actions remain present and enabled — the
       recovery copy never replaces the controls.
    5. Resolve all three brochure references — the head-level machine-discoverable pointer, the open
       action and the download action — and assert the three resolved destinations are equal to one
       another. Compare resolved destinations, never raw attribute strings: the head-level reference
       is relative on the live page, so a raw comparison would fail a correct page. Reuse the brochure
       path constant from the built-tree suite rather than retyping the literal, so a divergence
       between the built-tree assertion and the live assertion is impossible by construction.

    Record the name-to-destination table, the two counts, and the three resolved destinations through
    the shared recorder.
  </action>
  <acceptance_criteria>
    - The spec asserts a non-empty accessible name for every link, button and form control it traverses, and asserts the decorative-icon rule as a negative.
    - The spec asserts, for every destination-naming link, that the resolved destination equals the named destination, and asserts identical destinations for repeated identical names.
    - The spec asserts the capability count is exactly 10 and asserts the journey steps appear in document order.
    - The spec aborts the artifact route and re-asserts both content assertions plus the fallback subtree and both surviving controls.
    - The spec asserts the three resolved brochure destinations are equal to one another, using resolved destinations rather than raw attribute values.
    - `npx playwright test --project=live e2e/semantics.e2e.ts` exits 0.
    - `npm run typecheck` and `npm run lint` both exit 0.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck && npm run lint && npx playwright test --project=live e2e/semantics.e2e.ts && node -e "const s=require('fs').readFileSync('e2e/semantics.e2e.ts','utf8'); if(!/toHaveLength\(10\)|=== 10|toBe\(10\)/.test(s)) throw new Error('capability count equality not asserted'); if(!/route\(.*pdf|abort\(\)/.test(s)) throw new Error('artifact abort not exercised'); if(!/new Set\(|Set\(\[/.test(s)) throw new Error('three-reference equality not asserted');"</automated>
  </verify>
  <done>Names are descriptive and truthful, and the brochure's substance exists as HTML that outlives the brochure file.</done>
</task>

<task type="auto">
  <name>Task 3: Record the semantic evidence with its sequences, inventories and counts</name>
  <read_first>
    - `evidence/` — the heading sequences, landmark inventories, name table and equivalence counts written by the two preceding tasks
    - `.planning/phases/05-prove-the-deployed-journey/05-UI-SPEC.md` §"SS-2" — the recorded-as-observation instruction for the Products page's missing main landmark
    - `.planning/phases/04.2-split-haoo-into-its-own-repository-and-domain/04.2-VERIFICATION.md` §"Human Verification Outcome" — record measured values, not pass marks
  </read_first>
  <files>.planning/phases/05-prove-the-deployed-journey/05-EVIDENCE-SEMANTICS.md</files>
  <action>
    Write `05-EVIDENCE-SEMANTICS.md`.

    One section per state, each carrying the ordered heading-level sequence as it was captured — the
    literal sequence of levels with their texts, not a summary. State which states were captured live
    and name the two that plan 05-12 captures on the preview target, so the coverage is legible.

    A landmark inventory for each surface: the counts of each landmark type as integers, and the
    captured navigation names. A region inventory: the captured labelled-region name list beside the
    closed expected list, so a reader can compare them directly.

    A name-to-destination table: one row per destination-naming link, with the accessible name, the
    resolved destination, and the instance count. Include the two parent-site links explicitly, since
    they are the reason this rule exists.

    An equivalence section: the capability count found against the count expected as two integers,
    the journey-step count as two integers, the three resolved brochure destinations written out in
    full, and the aborted-artifact re-assertion results.

    An observations section, clearly separated from the assertions: the Products page's missing main
    landmark with its measured value, deferred by decision D-OQ-3 — a scope decision, not a severity
    judgement.

    Close with the two planner assumptions from the head of this plan, restated verbatim, so the
    unreachable-empty-case limit and the stated equality convention are visible in the evidence.
  </action>
  <acceptance_criteria>
    - `05-EVIDENCE-SEMANTICS.md` records a literal ordered heading-level sequence for each captured state, and names the states captured elsewhere.
    - The file records landmark counts as integers and shows the captured region list beside the closed expected list.
    - The file contains a name-to-destination table including both parent-site links with their resolved destinations.
    - The file records the capability and journey-step counts as found-against-expected integer pairs.
    - The file contains an observations section holding the Products page's missing main landmark, marked deferred under D-OQ-3.
    - The file restates the two planner assumptions carried by this plan, including the stated equality convention.
  </acceptance_criteria>
  <verify>
    <automated>grep -qi 'observation' .planning/phases/05-prove-the-deployed-journey/05-EVIDENCE-SEMANTICS.md && grep -q 'D-OQ-3' .planning/phases/05-prove-the-deployed-journey/05-EVIDENCE-SEMANTICS.md && grep -q 'www.zero-paperhub.com' .planning/phases/05-prove-the-deployed-journey/05-EVIDENCE-SEMANTICS.md && grep -q '10' .planning/phases/05-prove-the-deployed-journey/05-EVIDENCE-SEMANTICS.md</automated>
  </verify>
  <done>The semantic evidence carries literal sequences and integer counts, distinguishes assertions from observations, and states its equality convention in the open.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| accessible name → destination | A link's name is a promise to the visitor; its destination is the fulfilment, and nothing in HTTP validates the two against each other |
| brochure artifact → the page's content | The substantive content exists twice, once as HTML and once inside a binary artifact served from a separate path |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-43 | Spoofing | destination-naming links | high | mitigate | Every link whose name names a destination is asserted to resolve to that destination; a link promising the parent site and resolving elsewhere reddens the run, which is the defect class plan 05-04 fixed |
| T-05-44 | Repudiation | heading-order coverage | high | mitigate | The spec owns the assertion rather than delegating it to an advisory-tagged rule, asserts per state rather than once, and the file comments the signal that would mean the engine corroboration silently stopped |
| T-05-45 | Denial of service | brochure content availability | medium | mitigate | The HTML equivalent is asserted with the artifact route aborted, so content locked inside an unavailable file is a failing run rather than a silent loss of the brochure's substance |
| T-05-46 | Tampering | the three brochure references drifting apart | medium | mitigate | The three resolved destinations are asserted equal to one another and the path constant is reused from the built-tree suite, so one edited target cannot silently split them |
| T-05-47 | Information disclosure | `05-EVIDENCE-SEMANTICS.md` in a public repository | low | accept | The file records public headings, landmark names and destinations already served to every visitor |
</threat_model>

<verification>
- `npx playwright test --project=live e2e/semantics.e2e.ts` exits 0.
- `npm run typecheck` and `npm run lint` exit 0.
- The evidence file carries literal heading sequences, integer inventories, and the found-against-expected equivalence counts.
</verification>

<success_criteria>
- The live HAOO page reads correctly in document order, with one top-level heading and no skipped levels, in every state it can reach.
- Every name is descriptive, and every name that names a destination tells the truth.
- The brochure's content is available as HTML whether or not the brochure file is.
</success_criteria>

<output>
Create `.planning/phases/05-prove-the-deployed-journey/05-10-SUMMARY.md` when done
</output>
